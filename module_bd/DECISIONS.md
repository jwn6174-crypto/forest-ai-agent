# 모듈 B/D 의사결정 기록 (Architecture Decision Records)

이 문서는 *왜 이 선택을 했는지* 기록한다. 시연·대학원 지원·논문 Methods 섹션 활용용.

각 결정은 **상황 → 대안 비교 → 선택 근거 → 한계** 순서.

---

## D1: KOFPI 데이터 — 수종별 확장 (옵션 B)

**날짜**: 2026-05-13 (Day 2 후반)

**상황**:
가이드 §6.3 의 `market_snapshot()` 시그니처는 `timber_price` (소나무 기본 6 등급) 만 요구. 그러나 KOFPI 분기별 보고서에서 *7 수종 × 6 등급* 모두 추출 가능.

**대안 비교**:

| 옵션 | 내용 | 정확도 | 가이드 매칭 |
|---|---|---|---|
| A. 가이드 시그니처만 | 소나무 1수종 6 등급 | NPV 부정확 | 100% |
| B. 가이드 + 수종별 확장 | 7 수종 × 6 등급 | NPV 정확 | 100% (확장) |
| C. 수종별만 | 가이드 시그니처 무시 | NPV 정확 | 위반 |

**선택**: 옵션 B

**근거**:
- 수종별 가격 차이 *22-47%* (소나무 1등급 199,700원 vs 리기다소나무 105,400원)
- NPV 결정적 영향 → *시연 임팩트 큼*
- 가이드 시그니처 *유지* + *추가 정보 제공* = *호환성 + 정확성*
- 희도가 NPV 계산 시 `snap["timber_price_by_species"][species]` 호출 가능

**한계**:
- KOFPI 시계열 *4 분기 (12개월) 만* — 2014-2024 깊은 시계열은 *원본 PDF 추가 다운로드 필요*
- 단일 시점 가격 → *Faustmann real-price-constant 가정* 따름 (가이드 §6.3 의 표준)
- 미래 가격 예측 *안 함* (가이드도 안 함)

---

## D2: 표준품셈 → 비용 함수 — 옵션 Y (진짜 PDF 추출 + 간단 모델)

**날짜**: 2026-05-13 (Day 2 후반)

**상황**:
가이드 §7.1 은 *placeholder 값* 제공 (예: `harvest_unit_cost: 45,000원/m³`) 후 "(예시 — 실제 PDF 확인 후 입력)" 주석. 진짜 PDF (산림청 고시 제2025-82호, 257p) 다운로드 후 진단 결과:
PDF 는 원·m³ 단위 가격 아님.
실제 구조 = 소요인력 (인/단위) + 시중노임단가 (별도) + 할증률 (26개) 3 요소 모델.
예시:
PDF 에 있음: "벌채 1m³ → 보통인부 0.05 인 필요"
PDF 밖:    "보통인부 1인 1일 = 180,000원" (대한건설협회 별도 공포)
→ 실제 벌채비 = 0.05 × 180,000 × 할증률

**대안 비교**:

| 옵션 | 추출 범위 | 정확도 | 시간 |
|---|---|---|---|
| X. 완전 추출 | 50-100개 작업 + 26개 할증 + 자재단가 | 95-100% | 4-6시간 |
| Y. 간단 모델 | 주요 4-5개 작업 + 기본 3개 할증 | 75-85% | 1.5-2시간 |
| Z. Placeholder | 가이드 예시값 그대로 (PDF 안 씀) | 가이드 함정 그대로 | 30분 |
| W. 학습 메모 + 마무리 | 추출 안 함, 진단만 기록 | — | 45분 |

**선택**: 옵션 Y

**근거**:
- 가이드 §7.1: "(예시 — 실제 PDF 확인 후 입력)" → *확인* 만 요구, *완전 추출* 요구 *아님*
- 가이드 §8 (Week 5 통합): 시연 정확도 *80%+ 충분*
- `COST_PARAMS` *5개 변수만* — 옵션 Y 가 *5개 변수 진짜 PDF 기반* 제공 가능
- 옵션 X 는 *오버 엔지니어링* (Week 4-5 일정 초과)
- 옵션 Z 는 *PDF 다운받고도 안 씀* — 정우 마인드 ("정확한 걸 선호") 와 충돌
- 옵션 Y 는 *추후 옵션 X 로 확장 가능* — 함수 시그니처 동일, `COST_PARAMS` dict 값만 교체

**한계**:
- 정확도 75-85% (옵션 X 의 95%+ 보다 낮음)
- 시중노임단가 *가이드 추정값* (170,000-200,000원/인일) 사용 — 대한건설협회 자료 *별도 작업*
- 자재단가 (묘목·연료·기계 손료) *생략*
- 26개 할증률 중 *기본 3개만* (경사도, 이동거리, 작업시기)
- 추후 *옵션 X 로 확장* 시 코드 구조 *유지*, `COST_PARAMS` 만 교체

---

## D3: cost_function() — KOFPI 우선 + 표준품셈 보완 (5/5 진짜 데이터)

**날짜**: 2026-05-13 (Day 2 후반)

**상황**:
가이드 §7.1 의 5개 변수 (`harvest_unit_cost`, `skidding_cost_per_m3_km`, `transport_cost_per_m3_km`, `regen_cost_per_ha`, `admin_overhead`) 의 placeholder 값을 *진짜 PDF 데이터* 로 정정.

표준품셈 PDF (산림청 고시 제2025-82호, 257p) 진단 결과:
- 표준품셈은 *소요인력 (인/m³, 인/ha)* 만 제공. *km당 단가* 직접 없음.
- 즉 가이드의 *선형 단가 모델* (단가 × 거리) 과 *간접* 매칭.

KOFPI 분기별 보고서 진단 결과 (정우 발견 ⭐):
- 제4장 *거리별 운반비 산정내역* = *대운반비 (50km 구간)* + *소운반비 (m 구간)* 직접 제공.
- 가이드의 *선형 단가 모델* 과 *직접* 매칭.

**대안 비교**:

| 옵션 | 데이터 출처 | 정확도 | 가이드 매칭 | 시간 |
|---|---|---|---|---|
| Z. Placeholder | 가이드 추정값 그대로 | 가이드 함정 그대로 | 100% | 30분 |
| Y. 표준품셈만 | 표준품셈 + 노임 (간접 모델) | 75-85% | 80% | 2-3시간 |
| **Y+** (선택) | **KOFPI 우선 + 표준품셈 보완** | **95%+** | **100%** | **3-4시간** |
| X. 완전 추출 | 표준품셈 26개 할증 전체 | 95-100% | 100% | 6-8시간 |

**선택**: 옵션 Y+ (KOFPI 우선 + 표준품셈 보완)

**근거**:
- KOFPI 운반비 = *실제 시장 거래 가격* (산림청 분기별 조사)
- 표준품셈 운반비 = *공식 추정 가격* (재구성 필요)
- 가이드 §7.1 의 모델 (단가 × 거리) → KOFPI 가 *직접 매칭*
- 가이드 §7.1 의 *placeholder 함정* 발견 + 정정:
  - `skidding_cost_per_m3_km: 3,500원` → KOFPI 진짜 *9,300-18,400원/m³* (3-6배 차이)
  - `transport_cost_per_m3_km: 1,200원` → KOFPI 진짜 *14,600-36,200원/m³* (구간별)
- 정우 직감 "KOFPI 가 더 정확" — 검증 후 채택

**데이터 매핑 (최종)**:
| 변수 | 출처 | 데이터 |
|---|---|---|
| harvest | 표준품셈 p.59-60 + 대한건설협회 노임 | 벌목부 25.5 m³/인일 × 248,140원/인일 |
| skidding | KOFPI 보고서 p.44 (소운반비) | 9,300원/m³ (500-1km) ~ 18,400원/m³ (2-4km) |
| transport | KOFPI 보고서 p.43 (대운반비) | 14,600원/m³ (50km) ~ 36,200원/m³ (300km+) |
| loading | KOFPI 보고서 (상차비) | 5,800원/m³ (2025 Q4) |
| regen | 표준품셈 p.73 + 대한건설협회 노임 | 1.33 특별인부 + 20 보통인부 + 4,000본 묘목 |
| admin_overhead | 가이드 §7.1 | 0.15 |

**검증 결과** (강원소나무 1ha 개벌, 280m³, 도로 15km, 중경사):
- 총 비용: **21,599,743원**
- 단가별 분해: harvest 13.2%, skidding 12.7%, transport 18.9%, loading 7.5%, regen 34.6%, admin 13.0%
- 거리 영향: 5km vs 300km = 32% 비용 증가
- 산림조합 실제 견적과 *근접* — 현실적 수치

**한계**:
- 묘목 단가 (800원/본) 는 *추정값* — 산림용 종자·묘목 고시 별도 확인 필요
- 보육·간벌 *재투입* 비용 미반영 (개벌 단순화)
- KOFPI 운반비 *전국 평균* — 충북 보은 지역 특화 X
- 표준품셈 26개 할증 중 *경사도 (1-4-5) 만* 반영 — 작업시기·이동거리·집단화정도 등 추후
- 운반비 *구간형* (50km 단위) — 5km 와 30km 가 같은 단가 (KOFPI 표 구조 한계)

**시연 가치**:
- 가이드 §7.1 의 *placeholder 함정 발견 + 정정* 자체가 *학술 기여*
- 5/5 진짜 데이터 출처 추적 가능 → 심사위원·대학원 *반박 어려움*
- 정우의 *직감* 이 *프로젝트 결정적 분기점* — 데이터 우선 선택 패턴

---

## D4: shared/schemas.py — 가이드 §8.1 + 우리 확장 (옵션 P2)

**날짜**: 2026-05-13 (Day 2 마감)

**상황**:
가이드 §8.1 의 4개 Pydantic 모델 (GrowthForecast, MarketSnapshot, CostFunction, RotationRule) 을 정의해야 함. 그러나 우리가 구현한 함수의 *반환 데이터* 가 가이드 §8.1 명세보다 *풍부* 함:

- `market_snapshot()`: 가이드는 `timber_price` (소나무 6 등급) 만. 우리는 `timber_price_by_species` (7 수종), `timber_price_meta` (출처), `kau_meta` 등 *확장 자산* 보유.
- `growth_predict()`: 가이드는 `volume/dbh/height/n_per_ha trajectory`. 우리는 `tmai_trajectory`, `method`, `warning` 추가.
- `rotation_age()`: 가이드는 3 필드. 우리는 `legal_basis`, `species_category` 추가.
- `cost_function()`: 가이드의 `CostFunction` 명세가 *함수 입력 변수* 모델로 모호 → 우리는 `CostInput` (입력) + `CostBreakdown` (출력) *분리*.

**대안 비교**:

| 옵션 | 가이드 매칭 | 우리 자산 보존 | 복잡도 |
|---|---|---|---|
| P1. 가이드 명세 그대로 | 100% | 0% (자산 손실) | 단순 |
| **P2** (선택) | **100%** | **100% (Optional 확장)** | **중간** |
| P3. 우리 자유 설계 | 80% | 100% | 복잡 |

**선택**: 옵션 P2 — 가이드 §8.1 기본 필드 *100% 호환* + 우리 확장 필드 *Optional* 추가

**근거**:
- 가이드 호환성 100% → 희도 NPV 작성 시 *가이드대로 호출* 가능
- 우리 풍부한 자산 (수종별 가격, 출처 메타) *Optional 활용* 가능
- *후방 호환성* — 가이드 명세만으로도 모델 생성 가능 (검증 5개 통과)
- 정직히 *가이드 vs 우리 확장* 차이 *주석 명시* — 학술 투명성
- 정우 관점: "가이드는 *나침반*. 토씨 하나까지 따라야 하는 건 아님."

**결과 구조**:

```python
# 가이드 §8.1 매칭 (필수 필드)
class MarketSnapshot(BaseModel):
    date: str
    timber_price: Dict[str, Optional[int]]
    kau_close: Optional[float]
    koc_estimate: Optional[float]
    vcm_floor_wta: float = 17039
    discount_rate: float = 0.05
    
    # 우리 확장 (Optional)
    timber_price_by_species: Optional[Dict[str, Dict[str, Optional[int]]]] = None
    timber_price_meta: Optional[Dict[str, Any]] = None
    kau_meta: Optional[Dict[str, Any]] = None
```

**검증 결과** (5개 스키마, 자가 테스트):
- ✅ MarketSnapshot — 가이드 기본만으로 생성 가능
- ✅ MarketSnapshot — 우리 확장 포함 생성 가능
- ✅ RotationRule — 가이드 + 우리 확장
- ✅ CostInput + CostBreakdown — 입력·출력 분리
- ✅ GrowthForecast — `from_trajectory_dicts()` 헬퍼 작동

**한계**:
- `grade_distribution_trajectory` (가이드 §8.1) 는 Optional — Weibull fit 후 추후 채움
- `carbon_uptake_rate` (가이드 §8.1) 도 Optional — 산림탄소 모델 통합 추후

**시연 가치**:
- 가이드 §8.1 *100% 호환* — 심사위원 매칭 검증 가능
- *Optional 확장* 으로 우리 풍부한 자산 활용 가능
- 팀 협업 *진짜 인프라* — 희도·수범이 IDE 자동완성으로 *빠른 작업*

---

## D5: carbon_uptake_rate — growth_predict() 통합 (Day 3)

**날짜**: 2026-05-15 (Day 3)

**상황**:
가이드 §8.1 의 `GrowthForecast.carbon_uptake_rate: List[float]` 필드가 *Optional* 로만 있었음. Faustmann NPV 가 *탄소상쇄 수입* 을 계산하려면 *수종·임령별 탄소흡수량 (tCO2/ha/yr)* 데이터 필수.

또한 정우 발견 (Day 2): KAU 종가 (15,550원/tCO2) 가 박2020 WTA (17,039원/tCO2)에 8.7% 미달 — 돌파 임박 (2026-06-05 실데이터 정합 정정: 최신 검증값 15,550원). 시장 가격이 산주 의지가격에 *아직 진입 전, 임박* → 탄소상쇄 사업 *경제성 진입 직전*. 이걸 시연 시 *진짜 수치* 로 보여주려면 `carbon_uptake_rate` 통합 필수.

**대안 비교**:

| 옵션 | 데이터 출처 | 정확도 | 한국 매칭 | 시간 |
|---|---|---|---|---|
| C1. IPCC 2006 기본값 | 국제 일반 계수 | 70% | 낮음 | 30분 |
| **C2** (선택) | **국립산림과학원 표준** | **95%** | **100%** | **1-1.5시간** |
| C3. 학술 논문 개별 종합 | 연구마다 다름 | 변동 큼 | 보통 | 3-4시간 |

**선택**: 옵션 C2 — 국립산림과학원 표준 탄소흡수량 (2003 개발, 2013/2024 개정)

**근거**:
- 한국 *공식 국가 표준* — UNFCCC IPCC 표준법 + 한국 자료 (3,212 표본 × 40년)
- 주요 8개 수종 (소나무, 잣나무, 낙엽송, 리기다, 편백, 참나무 등) 직접 매핑
- 임령별 (10/20/30/40/50/60년) 데이터 → 선형 보간으로 임의 임령 가능
- 산림청·환경부 공식 인용 가능
- *학술적 반박 어려움*

**구현 방식**:
1. `module_bd/data/raw/carbon/carbon_uptake_2003.json` 학술 자산 보관 (출처·법적 근거·단위 명시)
2. `_lookup_carbon_uptake(species, age)` 헬퍼 함수 추가 (`growth_predict.py`)
3. `growth_predict()` 의 trajectory dict 에 `carbon_uptake_rate` 추가
4. 수종 별칭 매핑 (강원소나무 → "소나무", 신갈/굴참/상수리 → "참나무" 등)
5. 임령 보간 (10년 단위 데이터 → 임의 임령) — 선형 보간

**검증 결과** (강원소나무 SI=14):
- 30년: 10.77 tCO2/ha/yr (탄소 흡수 *피크*) ✅
- 40년: 7.19 (감소 시작)
- 50년: 4.92 (피크의 46%, *감소*)
- 60년: 7.35 (별도 출처 데이터)
- 잠정 수종 (백합나무, 자작나무): 데이터 없음 → 정직한 warning ✅

**시연 인사이트** (이 통합으로 가능):
- "벌채 후 재조림 → 젊은 숲 유지 → 탄소 ↑" *학술적 증명*
- KAU 15,550원 × 흡수량 = *진짜 탄소 수입 시뮬레이션* (2026-06-05 실데이터 정합 정정: 최신 검증값 15,550원)
- 30년 vs 50년 NPV 비교 시 *탄소 수입* 까지 합산 가능
- 가이드 §6.4 의 *산림탄소상쇄 사업 논리* 정확 매칭

**예시 NPV 영향** (강원소나무 1ha, 50년까지 키울 시):
- 20년간 평균 흡수: 약 7.6 tCO2/ha/yr × 20년 × 1ha = 152 tCO2
- 탄소 수입: 152 × 15,550원 (KAU) = 2,363,600원 (2026-06-05 실데이터 정합 정정: 최신 검증값 15,550원)
- 원목 수입 대비 약 5% 추가 (현재 가격 기준)
- *KAU 가격 상승 시 비중 ↑*  → 정책 시뮬레이션 *진짜 가능*

**한계**:
- 60년 초과 임령: 60년 값 *외삽* (extrapolation, warning 명시)
- 자작나무·백합나무: 국립산림과학원 데이터 없음 → `None` + warning
- 지역 차이 미반영 (전국 평균만)
- 토양 탄소 미반영 (지상부 바이오매스만)
- 산불·병해충 *기대 손실* 미반영
- KOFPI 평균값 — 충북 보은 특화 X

**가이드 매칭**:
- §8.1 의 `carbon_uptake_rate: List[float]` 필드 *완벽 채움*
- §6.4 의 산림탄소상쇄 사업 *데이터 기반 검토 가능*

**다음 작업과 연결**:
- 다음 단계: 희도 모듈 C 에서 `carbon_uptake_rate` 활용 NPV 계산
- 추후 보완: SSP 기후 시나리오 시 탄소 흡수량 *변동 계수* 추가

---

## D6: 묘목 단가 — 산림청 2025 공식 매핑 (cost_function 정밀화)

**날짜**: 2026-05-15 (Day 3)

**상황**:
cost_function() 의 `REGEN_SEEDLING_UNIT_COST` 는 *800원/본 추정값* 이었음 (D3 의 알려진 한계). 산림청 *2025년 산림용 종자 및 묘목가격 고시* 직접 방문 → 진짜 공식 데이터 확보.

**핵심 발견**:
- 산림청 공식 페이지 (robots.txt 자동 fetch 막힘) → 정우가 *직접 브라우저 방문*
- 2025년 *성묘 (조림용)* 가격 표 확보
- 단위: 원/천그루 (1,000으로 나눈 값 = 원/본)
- 법적 근거: 산림자원의 조성 및 관리에 관한 법률 시행령 제16조

**대안 비교**:

| 옵션 | 데이터 출처 | 정확도 | 시간 |
|---|---|---|---|
| Z. 기존 추정 (800원/본) | 가이드 추정값 | ±30% | 0 |
| C1. 시중 묘목상 가격 | 종묘회사 가격표 | 정확하지만 변동성 ↑ | 어려움 |
| **C2** (선택) | **산림청 공식 2025** | **95%+** | **20분** |
| C3. 학술 논문 | 연구별 다름 | 변동 | 1-2시간 |

**선택**: 옵션 C2 — 산림청 2025년 *성묘* 공식 가격 (1-1 표준)

**근거**:
- 한국 *공식 국가 가격* (시행령 제16조 매년 결정·고시)
- 검사합격률 95% 이상 + 재해손비 + 의무보험료 포함 (실제 조림 시 사용 가격)
- *15 수종 매핑* — 우리 7+ 수종 모두 커버
- 학술적 인용 가능

**데이터 매핑** (1-1 = 1년생 + 1회 상체, 조림 표준):

| 수종 | 단가 (원/본) | 이전 추정 대비 |
|---|---|---|
| 강원지방소나무 | 422 | -47% (800 → 422) |
| 잣나무 (2-1) | 530 | -34% |
| 낙엽송 | 714 | -11% |
| 리기다소나무 | 388 | -52% |
| 편백 | 665 | -17% |
| 상수리나무 (참나무류) | 1,101 | +38% |
| 백합나무 | 1,219 | +52% |
| 자작나무 | 1,093 | +37% |

→ 평균: 약 550원/본 (수종 미제공 시 기본값)

**구현 방식**:
1. `module_bd/data/raw/seedling/seedling_prices_2025.json` 학술 자산 보관
2. `cost_function.py` 의 `SEEDLING_PRICES` dict 정의 (15 수종)
3. `cost_function()` 시그니처에 `species: str = None` 파라미터 추가
4. regen 계산에서 수종 매핑 → 미제공 시 *평균 (550원/본)* 사용

**검증 결과** (강원소나무 1ha 개벌, 280m³, 도로 15km, 중경사):
- 이전 (800원/본):  총 21,599,743원
- 정정 (422원/본):  총 19,774,003원 (-8.5%)
- regen 항목:       7,475,277원 → 5,887,677원 (-21%)

**시연 인사이트** (수종별 조림비용 차이):
- 가장 싼 리기다소나무 (388원/본):  15,894,590원
- 가장 비싼 백합나무 (1,219원/본):   19,908,320원
- 차이: 25.2% (수종 선택이 비용 결정 변수)

**한계**:
- 산림청 공식 = *국가·지자체 조림용* 가격 (시중 산주 거래가는 1.2-2배 가능)
- *분뜨기 묘목* (대형, 4-8백만원/천그루) 별도 — 우리는 *노지묘* 만 사용
- 비료·운반비 200,000원/ha 는 여전히 *추정값* (표준품셈 "별도 계상")
- 잣나무 외 *상체 횟수* 영향 X (2-2, 2-3 등 다양한 묘령 미세 차이)

**가이드 매칭**:
- §7.1 `regen_cost_per_ha` placeholder 의 *마지막 추정* 정정
- *5/5 진짜 PDF/공식 데이터* 완성

**다음 작업과 연결**:
- cost_function() 의 *모든 변수* 진짜 데이터 → 희도 NPV 정확도 ↑
- 산주가 *수종 전환* 시나리오 비교 가능 (소나무 ↔ 참나무 ↔ 백합)

---

## D7: 산림탄소상쇄 RAG 코퍼스 — 가이드 §6.4 완성 (Day 3 마감)

**날짜**: 2026-05-15 (Day 3)

**상황**:
가이드 §6.4 명시: "산림청 공표 PDF 6종 (식생복구·산림경영·재조림·신규조림·BCDM·산지전용)을 chunking 해서 Person 4 의 RAG 에 전달". 그러나 *진짜 PDF 6종이 어디 있는가?* 가 모호 — 진짜 출처는 산림탄소등록부 (carbonregistry.forest.go.kr).

또한 산림탄소등록부 사이트는 *robots.txt 막힘* — 자동 fetch 불가. D6 패턴 (정우 직접 방문) 재적용.

**핵심 발견**:
- 산림탄소등록부 *법령/표준/지침* + *방법론* 메뉴에서 11+ PDF 다운로드 가능
- 가이드의 "6 PDF" → 실제 *8 사업유형 방법론* + *3 보조 가이드* 구조
- 가이드 BCDM (Baseline 방법론) → 각 방법론 PDF 내부에 통합

**대안 비교**:

| 옵션 | 데이터 | 청크 수 | 가이드 매칭 |
|---|---|---|---|
| R1. 가이드 §6.4 6 PDF 만 | 6 사업유형 PDF | ~120 | 100% |
| **R2** (선택) | **8 사업유형 + 3 보조 = 11 PDF** | **281** | **확장 100%** |
| R3. 전체 21 PDF (2020 + 2025) | 중복 포함 | ~500 | 과잉 |

**선택**: 옵션 R2 — *2025.1.2. 기준 최신 8 사업유형 + 3 보조 PDF*

**근거**:
- 가이드 §6.4 의 6 사업유형 *모두 포함* + *2 추가* (목제품 이용, 산림바이오매스):
  - 가이드 *6 항목*: 식생복구, 산림경영, 재조림, 신규조림, BCDM, 산지전용
  - 실제 *8 사업유형*: 신규조림·재조림 통합, 벌기령 연장 산림경영, 식생복구, 목제품 이용, 산림바이오매스, 수종 갱신, 산불피해지 조림, 산지전용 억제
- 2020 버전은 *이전 버전*, 2025 가 *최신*
- 3 보조 PDF (운영지침, 산림조사, 통합가이드북) = 산주 *자주 묻는 질문* 데이터

**구현 방식** (가이드 §6.4 기반):
1. `module_bd/data/raw/carbon_offset/` 학술 자산 보관 (21 PDF + 5 HWP)
2. `carbon_offset_chunk.py` 의 `chunk_pdf()` 함수 — 가이드 §6.4 코드 기반:
   - 한국어 문장 종결 (`(?<=[.!?。])\s+`) 으로 분리
   - 청크 최대 800 글자
   - Overlap 2 문장
3. 11 PDF chunking → JSONL 281 청크
4. memory 관리 (`gc.collect()` 50 페이지마다)
5. JSONL 형식: `{text, source, page, project_type, type}`

**검증 결과**:
- 총 281 청크 생성 (평균 570 자, 최소 5, 최대 2,089)
- 사업유형별 분포 균형:
  - 통합 가이드북: 66 청크 (입문서)
  - 산림조사 가이드라인: 42 청크
  - 운영 지침 2024: 38 청크
  - 벌기령 연장 산림경영: 29 청크
  - 산지전용 억제: 19 청크
  - 신규조림재조림: 17 청크
  - 식생복구: 17 청크
  - 산림바이오매스: 15 청크
  - 산불피해지 조림: 15 청크
  - 수종 갱신: 14 청크
  - 목제품 이용: 9 청크
- 샘플 청크 *깨끗한 한국어*, 구조 보존 (방법론명, 적용조건 등)

**Person 4 (수범) 활용 방식**:
- `carbon_chunks.jsonl` 직접 사용
- embedding (`sentence-transformers/all-MiniLM-L6-v2`) + FAISS 벡터 DB 구축
- 산주 자연어 질문 → 관련 청크 검색 → LLM 응답 생성

**시연 인사이트** (이 데이터로 가능):
- "30년생 소나무 산림을 5년 더 키우면 탄소상쇄 사업 가능?"
  → `forest_management_rotation` (29 청크) 검색
- "벌채 후 재조림 시 어떤 사업 가능?"
  → `afforestation_reforestation` (17 청크)
- "산불 후 우리 산 어떻게?"
  → `fire_damage_planting` (15 청크)

**한계**:
- HWP 파일 (5개 타당성 평가 편람) 미처리 — pdfplumber X
- *모음집 PDF* (50MB) 제외 — 8 사업유형 PDF 와 중복
- 청크 *embedding 미생성* — Person 4 책임
- 청크 *검증·인증 절차 안내* 이지만 *수치 추출 X* — RAG 의 한계

**가이드 매칭**:
- §6.4 *완전 구현* (PDF 6 → 11 확장, 청크 형식 정확)
- §1.3 *Person 4 위 chunked PDF* 책임 *완성*
- 추후 *embedding 단계는 Person 4 책임* (가이드 §1.3 명시)

**다음 작업과 연결**:
- 수범 Module E 가 `carbon_chunks.jsonl` 받아 RAG 시스템 구축
- 산주 자연어 답변에서 *진짜 데이터 + 진짜 정책* 통합 가능

---

## D8: 산악기상 시계열 — 데이터 소스·수집 설계 (Day 3-5, 완료)

**날짜**: 2026-05-16 (Day 3 시작) ~ 2026-05-22 (Day 5 수집 완료)
**상태**: ✅ 수집 완료 — 6/6 관측소, 53,788 ok / 3,884 결측 (93.3%)

**상황**:
가이드 §2.3 은 임분수확표의 기후 보정을 위해 산악기상 시계열
(연평균기온·강수량·VPD)을 요구. growth_predict() 의
climate_scenario 파라미터가 baseline 외에는 미작동 상태였음.

**데이터 소스 결정**:
- 산림청 국립산림과학원 산악기상정보 API (data.go.kr 1400377/mtweather)
- DATA_GO_KR_KEY 로 접근 (별도 키 불필요)
- 충북 보은 인근 6 관측소 선정 (가덕산·금단산·염동산·시루산·삼승산·노성산,
  고도 242~627m — 강원소나무 분포대와 일치)

**진단으로 확인된 제약**:
- tm(관측시간) 파라미터 필수 — 기술문서엔 "옵션"이나 실제로는
  지정해야 데이터 반환. 1 호출 = 1 시점.
- 과거 데이터 범위: 2022-01 부터. 2021년 이전은 전 기간 없음
  (보은 관측소가 2022년경 신설). → 가용 4년치로 확정.
- 일일 호출 한도 1만 건 (HTTP 429). 6시점×6관측소×1,596일=57,456 호출
  → 한도 6일치. 매일 나눠 수집.

**시점 설계 — 하루 6시점 (옵션 A)**:

| 옵션 | 시점 | 일평균 정확도 | 비고 |
|---|---|---|---|
| 정오 1시점 | 12시 | 일최고에 편향 | 습도·VPD 왜곡 |
| 2시점 | 06/15시 | (min+max)/2 표준 근사 | 충분, 기상학 정식 |
| **A (채택)** | **04/05/06+14/15/16** | **최저·최고 시각 보정** | **6일 수집** |

선택: 옵션 A. 2시점도 우리 목적(NFI 잔차 회귀 입력)엔
충분하나, 최저·최고 시각이 빗나가는 약점을 새벽·오후 각
3시점으로 보완. 정확도 우선 판단.

**한계 / 미완 사항**:
- 수집 완료(6/6 관측소, Day 3~5, 5일 분할): 각 관측소 9,594 시점 (95%+).
  결측 패턴 2 그룹으로 분리: 그룹 A 안정 (가덕산·금단산·염동산, 결측
  1.5~5.5%), 그룹 B (시루산·삼승산·노성산, 2024-07~11 통째 결측 5개월,
  추정 원인 지역 통신망/공통 관리 시스템). D10 (전처리)에서 결정 4 C
  강건성으로 보강.
- climate_correct() 회귀는 NFI 실측 데이터 필요 → 모듈 A 협업
  시점까지 보류. 현재 growth_predict() 는 baseline 으로만 작동
  (가이드 §10.3 상 학술적으로 valid).
- 강수량(rn)은 시점 샘플링이라 일강수 누락 — 기온 중심 baseline,
  강수는 추후 SSP/격자자료로 보완.
- API 코드는 catch-up 재실행 지원 (429 시 무손실 중단).

---

## D9: 임가경제 데이터 — 임업 다각화 보조 수입 (Day 4)

**날짜**: 2026-05-19 (Day 4)

**상황**:
가이드 §3.1 은 Faustmann NPV 의 '임업 다각화 보조 수입' — 산주가
원목·탄소 외 임업으로 버는 연간 소득 — 을 요구. 모듈 C(희도)의
NPV 계산에 보조 현금흐름으로 들어감.

**데이터 경로 — 3개 경로 검증**:

| 경로 | 결과 |
|---|---|
| KOSIS 지표 서비스 (15127763) | 임가인구·가구 수만 제공, 임가소득 없음 → dead-end |
| data.go.kr 산림자원통계 API (15080832) | 통계 카탈로그 검색 전용 — 수치 미제공 |
| 산림임업통계플랫폼 (엑셀 다운로드) | 임가경제조사 엑셀 확보 가능 → **채택** |

산림자원통계 API 진단(frsas_probe.py): 엔드포인트가
selectStatList1 하나뿐이며, 다른 엔드포인트 6종 시도 전부 HTTP
404. statSeq 등 파라미터 8종으로도 특정 통계 수치 조회 불가 —
"이런 통계가 있다"는 목록만 제공. KOSIS 지표 서비스와 동일 한계.

**확보 데이터**: 충북 임가경제조사 2020~2024 (단위 천원, 충북
표본임가 평균).
- 임업손익계산서(충청북도) — 품목별 임업수입·비용·당기순익
- 주요지표(월별지표) — 임가소득 전체 구조(임업·임외·이전소득 등)

**핵심 결정 — 두 출처의 '임업소득' 정의 차이**:
- 주요지표: 임업소득 = 임업총수입 − 임업경영비 (가구 단위, 단순).
- 손익계산서: 당기순익 = 수익 − 비용(감가상각비 등 포함, 회계 손익).
- 같은 명칭이나 산식이 달라 값이 다름 — 버그 아님. 2024년 기준
  주요지표 10,461 vs 손익계산서 10,127천원.

**채택**: Faustmann 보조 수입 = 주요지표 '임업소득' 5년 평균
**11,224천원/년**. 가구 단위·정의 단순·임가소득 구조 안에서
일관되어 baseline 으로 적합. 손익계산서는 품목별 수입(밤·대추·
표고·조경수 등) 세부를 담은 보조 자료로 보존.

**구현 방식**:
1. `module_bd/data/raw/forest_household_economy/` 에 엑셀 2종 보관
2. `forest_household_parse.py` — 두 엑셀 파싱:
   - 주요지표: r6('년' 행), 연도 블록 19칸 간격으로 5년 추출
   - 손익계산서: 행 라벨로 임업수입·유동비·고정비·당기순익 추출
3. `forest_household_economy.json` 산출 — by_year_indicators,
   by_year_pnl, five_year_average, 정의 차이 주석 포함

**5년 평균 (천원)**: 임가소득 37,784.8 / 임업소득 11,224.0 /
임업총수입 22,555.0 / 임업경영비 11,331.6.

**한계**:
- 충북 시·도 단위 표본 평균 — 보은군 단위 아님.
- 산림자원통계 API 가 목록 전용 → 자동 갱신 불가, 엑셀 수동
  다운로드 의존.
- 표본임가 = 임업이 주 소득원인 가구. 일반 산주의 부업적 임업
  수입과는 차이 있을 수 있음.

**가이드 매칭**:
- §3.1 의 임가경제 데이터 *확보 완료*.
- 모듈 C 가 by_year_indicators 또는 five_year_average 직접 활용.

---

## D10: 산악기상 시계열 전처리 — 임지 단위 일/월/연 통계 (Day 5)

**날짜**: 2026-05-24 (Day 5)

**상황**:
D8 의 산악기상 수집(2026-05-22 완료) 후, 가이드 §2.3 의 "전처리"
책임을 완수해 §5.4 climate_correct() 의 입력 (연 기후변수) 을
만든다. 보은 6 관측소, 53,788 ok 시점 (93.3% 성공) → 임지 단위
일/월/연 통계로 집약.

**설계 결정 — 5가지** (가이드 §2.3 본문은 일평균 정의 등 미명시,
우리 재량):

| 결정 | 채택 | 대안 | 근거 |
|---|---|---|---|
| 1. 기온 변수 | tm2m (2m) | tm10m (10m) | 식생 영향 높이. 임분수확표 잔차 의미 |
| 2. 일평균 | (min(04-06)+max(14-16))/2 | 6시점 단순평균, 06+15시 | 기상학 정식 (일최저·최고 평균). 결측 유연: 한쪽 그룹 1개라도 있으면 산출 |
| 3. 강수 | 제외 | rn 6시점 합 / cprn 누적 | rn 은 6시간만 측정 (18시간 누락). cprn 실측 검증 시 시간별 합과 불일치, 정체 불명 |
| 4. 임지 평균 | 가용 관측소 단순평균 | NaN / 그룹 A 보강 / 결측일 폐기 | 결정 4 강건성 실증 (아래) |
| 5. 출력 | 일/월/연 csv 3개 | 연만 | climate_correct 입력은 연. 검증·시각화·후속 분석을 위해 일/월도 보존 |

**핵심 발견 — 결정 4 C 강건성 실증**:
2024년이 부분 결측 가장 심한 해 (n_days 348, partial 164/full 184)
임에도 연평균이 다른 해와 거의 동일:
- 2022: 11.81°C (full 362, partial 3)
- 2023: 12.76°C (full 346, partial 19)
- 2024: 12.79°C (full 184, partial 164)
- 2025: 12.79°C (full 324, partial 41)

2023 vs 2024 연평균 차이 0.03°C. 그룹 B 결측이 연평균에 실질 영향
없음 — climate_correct 의 연 통계 입력으로 4년 모두 valid.

**검증 — 청주 평년과 비교**:
- 완전한 해(2022/2023/2025) 평균 12.45°C
- 청주 기상청 평년 12.5°C, 보은 6관측소 평균 고도 359m → 기온감률
  (0.6°C/100m) 보정 ≈ 10.3°C
- min+max/2 편향: 일평균이 진짜 일평균보다 약 1~2°C 높음 (알려진
  기상학 편향, 낮 시간 비중이 큼)
- 예상 범위 11.3~12.3°C, 실측 12.45°C — 합리적 범위 내. 데이터
  학술적으로 건강.

**산출** (data/processed/):
- mt_weather_daily.csv    1,585행 (4년+5개월 임지 일평균)
- mt_weather_monthly.csv  53행 (2022-01 ~ 2026-05)
- mt_weather_annual.csv   5년 (climate_correct 입력 후보)

**한계 / 후속**:
- 강수 시계열 미구축 — 추후 SSP/격자자료(NEX-GDDP-CMIP6) 로 보완.
- 2026년은 부분 데이터(1~5월, n_days 142) — climate_correct 입력
  부적합. 4년(2022-2025) 만 사용.
- climate_correct 회귀 자체는 NFI 잔차 필요 → 모듈 A 협업 시점에.
- 6관측소 모두 한쪽 그룹 통째 결측인 18일(1.1%) 산출 불가 —
  수집 한도 도달 영향 추정. 무시 가능 수준.

**가이드 매칭**:
- §2.3 "산악기상 시계열 수집·전처리" 책임 완수 (D8 수집 + D10 전처리).
- §5.4 climate_correct() 입력 자료 준비.
- §10.3 "강수는 부차적" 톤과 일치.

---

## D11: NFI 7차 데이터 — 단위·구조·지침서 동등성 결정 (Day 6, 완료)

**일자**: 2026-05-27 (Day 6)
**책임**: 정우 (모듈 B/D)
**관련 가이드**: §1.2 책임 4·5번 (climate_correct, 등급분포 Weibull), §3.1 NFI 데이터

### 배경

NFI raw 데이터를 동료로부터 5·6·7차 xlsx 형식으로 확보 (5/27). 7차(2016-2020)
가 산악기상 시계열(2022-2026)에 가장 시기적으로 가까워, 본 분석의 *기본 데이터셋*
으로 채택. 분석 시작 전 *데이터 구조·단위*를 명확히 정의하여 후속 작업(climate_
correct, Weibull fit)의 *원자 단위 오류*를 사전 차단.

### 사용 가능한 지침서

- **제6차 국가산림자원조사 현지조사지침서 Ver 1.1** (산림청 국립산림과학원, 2011)
- **2020 한국의 산림자원 — 국가산림자원조사 개정판** (2022, 부록 B에 7차 지침서 포함)

### 결정 1: 6차 vs 7차 지침서 동등성

**결정**: 6차 지침서를 *기준 문서*로 사용. 7차 보고서 본문·부록은 *보강 자료*로 사용.

**근거**:
- 7차 보고서가 부록 B 로 *7차 현지조사 지침서*를 포함 (p.115+).
  6차에서 7차로 표 형식·구조 *일부 갱신*. 측정 항목·단위·코드는 *변동 없음*.
- 7차 보고서 본문이 6차 지침서의 표 (영급 10년 단위, 임상 D/H/M, 경급 1-4 등) 를
  *그대로 인용*하여 통계 산출.
- 6차 지침서가 *접근 가능한 PDF 형태*로 가장 자세함. 본문 §4.3-§4.4 가
  측정 항목·단위·표준 절차를 명시.

**결론**: 6차 지침서의 측정 단위·코드 정의가 7차 데이터에도 *그대로 적용됨*.

### 결정 2: 길이 컬럼의 단위 — *실측 데이터 검증*

6차 지침서의 한국어 표현 "10 cm 단위로 기록" 이 *모호함*:
- 해석 X: "cm 단위로 기록, 측정 정확도 ±10cm"
- 해석 Y: "10cm 자체가 1단위 (즉 13 = 130cm)"

**검증**: NFI 7차 보은 4,505 그루의 *수고* 실측 범위 보고:
- 값 범위: 370 ~ 3,030 (유효 855/4,505)
- 해석 X 적용: 3.7 ~ 30.3m (평균 13.58m)
  → 한국 임분 평균 수고 12-16m 와 *완벽 매칭*. **물리적으로 정상**.
- 해석 Y 적용: 37 ~ 303m
  → 303m 는 *세계 최고 높이 레드우드(120m)의 2.5배*. **물리적으로 불가능**.

**결정**: 해석 X 채택. 즉 *NFI 값 자체는 cm 단위*, 분석 시 ÷100 = m.

**보강 근거** (6차 지침서 §4.4.6.4 표준목 측정):
"지하고는 cm단위로 측정한다 ... 수고는 측고기를 사용하여 cm단위로 기록한다"
→ §4.4.5 의 "10 cm단위로 기록" 이 §4.4.6.4 의 "cm 단위로 기록" 과 *동등*.
"10 cm 단위" = "cm 단위로 기록, 정확도 10cm 까지" 의 의미.

### 결정 3: 영급 코드 해석 (10년 단위)

**결정**: 영급 코드 N (1~10) → 임령 범위 (10(N-1)+1) ~ (10N) 년.
- 1영급 = 1-10년, 2영급 = 11-20년, ..., 5영급 = 41-50년, ..., 10영급 = 91-100년.

**근거**: 6차 지침서 §4.3.3.4 표12. 7차 보고서가 영급 정의를 *그대로 유지*하며
2020년 기준 통계 산출에 적용 (보고서 §1, §3 인용).

**보은 4-5영급 (31-50년) 67개 = 보은 표본점 102개의 66%** — 우리 핵심 분석 영역.

### 결정 4: 수령 컬럼 무시 — 영급으로 대체

**결정**: NFI 임목조사표의 *수령* 컬럼은 표본점당 5~10본 표준목만 측정.
99.9% 결측. 분석 시 *수령 무시*, 표본점 단위 *영급 사용*.

**근거**: 6차 지침서 §4.4.6.4:
"표준목 중에서 각 직경급별로 고르게 분포하도록 5본을 선정하여 측정 ...
생장추로 흉고직경 측정부위에서 ... 목편 채취하여 연륜수를 파악"
→ 수령은 *표준목 한정 측정*. 모든 나무는 *영급*으로만 임령 추정.

가이드 §10.2 (Plan B) 도 동일: "임상도 영급(I~VI, 10년 단위)으로만 알 수 있음."

### 결정 5: 단위 변환 표 — 우리 분석 표준

| 컬럼 | NFI 저장 단위 | 우리 분석 변환 | 검증 (보은 실측) |
|---|---|---|---|
| 흉고직경(DBH) | cm | 그대로 | 6.0 ~ 64.0 ✓ |
| 수고 | cm (10cm 정확도) | ÷100 = m | 평균 13.58m ✓ |
| 지하고 | cm (10cm 정확도) | ÷100 = m | (미진단) |
| 거리 (표본점 내) | m 또는 cm | 실측 확인 후 결정 | (미진단) |
| 방위각 | 도(°) | 그대로 | — |
| 해발고 | m | 그대로 | 96 ~ 678m ✓ |
| 경사 | 도(°) | 그대로 | 7 ~ 44° ✓ |
| 추정간재적 | m³ | 그대로 | 0.008 ~ 2.9m³ ✓ |

### 결정 6: 코드 매핑 — 우리 분석 표준

| 컬럼 | NFI 코드 | 해석 |
|---|---|---|
| 임상 | D/H/M | 침엽수림(D, ≥75%) / 활엽수림(H, ≥75%) / 혼효림(M, 25~75%) |
| 경급 | 1/2/3/4 | 치수<6cm / 소경6-18 / 중경18-30 / 대경≥30cm (DBH 단면적 51%+) |
| 영급 | 1~10 | 10년 단위 (1=1-10년, 10=91-100년) |
| 수관밀도 | 1/2/3/4 | 0-25% / 26-50% / 51-75% / 76-100% |
| 수관급 | a/b/c/d/e | 우세목 / 준우세목 / 중층목 / 피압목 / 폭목 |
| 형질급 | 1/2/3 | 1급 (최고) / 2급 / 3급 (피압목 포함) |

### 결정 7: 보은 분석 가능성 검증 — 가이드 추정과 비교

| 항목 | 가이드 §3.4 추정 | NFI 7차 실측 | 비고 |
|---|---|---|---|
| 보은 표본점 수 | 27~30 (4×4km 격자) | **102** | 3.6배. 집락 구조 (1집락=3-5표본점) 차이 |
| 충북 표본점 수 | 명시 X | 1,162 (12 시군) | Weibull 도 단위 fit 가능 |
| 보은 개별 나무 | 명시 X | 4,505 그루 | DBH 100% 유효. Weibull fit 강건 |
| 보은 수종 다양성 | 11 수종 (우리 분석 대상) | 49 수종 (NFI 전체) | 11 수종 모두 포함 |
| 임상 분포 | 명시 X | 활엽수 39 / 혼효림 34 / 침엽수 16 / 결측 13 | 다양 |
| 영급 분포 | 명시 X | 4-5영급 67개 (66%) | 핵심 영역 충분 |
| 해발고 분포 | 명시 X | 96-678m (평균 336m) | 산악기상 관측소 242-627m 매칭 |

**결론**: 가이드 추정의 3.6배 표본 자산. climate_correct R² 0.5-0.7 기대 가능.
Weibull fit *수종·영급별 sub-fit* 가능. 발표·논문 임팩트 큼.

### 보안 결정 (D8 의 NFI 차단 보강)

- raw xlsx 3개 (총 약 340MB): `.gitignore` 3중 차단 (5/24 커밋 a1cf786).
- 추출본 csv 저장 시 *옵션 C* 추정: 좌표 살리되 `data/raw/nfi/` 안에만,
  git 차단 유지, *집계 결과* 만 git 으로 산출. → 다음 결정(D12)에서 확정.

### 후속 결정 예고

- **D12**: NFI 추출본 csv 저장 방식·좌표 처리 옵션.
- **D13**: climate_correct() 회귀 설계 (gradient boosting, 입력 변수, 검증).
- **D14**: 등급분포 Weibull fit 설계 (수종·영급 그룹, 모수 추정 방법).

### 가이드 매칭

- §1.2 책임 4·5번 (climate_correct, Weibull) 의 *데이터 input* 확정.
- §3.1 NFI 데이터 정의 (좌표·임령·DBH·수고·재적) 모두 매핑됨.
- §10.2 Plan B (수령 결측 시 영급 사용) 적용 준비됨.

---

## D12: NFI 7차 추출본 csv 저장 방식 + 보은 깊은 진단 (Day 6, 완료)

**일자**: 2026-05-27 (Day 6)
**책임**: 정우 (모듈 B/D)
**관련 가이드**: §1.2 책임 4·5번 (climate_correct, Weibull), §3.1 NFI 데이터

### 배경

D11 에서 NFI 7차 단위·구조를 검증한 뒤, 본 분석(climate_correct, Weibull)에
*매번 124MB xlsx 로딩*은 비효율적. 추출본 csv 산출 + 보안 결정 + 보은 데이터의
*분포·매칭* 깊은 진단을 동시 수행하여 D13·D14 설계의 *직접 입력* 확보.

### 결정 1: 추출 범위 — 충북 전체

**결정**: 충북 1,162 표본점 / 46,722 그루 추출. 보은만 102/4,505 도 동시 사용 가능.

**근거**:
- 보은만 추출 시 — climate_correct OK, Weibull *영급별 sub-fit* 가능 (D11 결정 7).
- 충북 전체 — Weibull *수종·영급 강건 fit* (더 정밀). csv 크기 부담 없음 (5MB).
- 모듈 A 가 *보은만 학습*하더라도, 우리 분석은 *충북 fallback* 가능.

### 결정 2: 차수 — 7차 우선, 5·6차 보류

**결정**: NFI 7차(2016-2020) 만 우선 추출·분석. 5·6차는 시계열 분석 보너스로 추후.

**근거**:
- 7차가 산악기상 시계열(2022-2026) 과 시기적으로 가장 가까움.
- 한 차수 끝낸 뒤 같은 코드로 5·6차 적용 가능 (재사용).
- 한 번에 다 하면 인지 부담 ↑.

### 결정 3: 좌표 처리 — 옵션 A (그대로 보존)

옵션 비교:

| 옵션 | 좌표 | 분석 정확도 | 보안 위험 | 채택 |
|---|---|---|---|---|
| A | 그대로 (소수점 2자리) | 최대 | raw/nfi/ 차단으로 0 | ✓ |
| B | 1km 격자로 반올림 | 미세 손해 | ↓ | |
| C | 좌표 컬럼 제외 | 큰 손해 (임지 매칭 불가) | 0 | |
| D | 해시값 | 매핑 표 필요 | 복잡 | |

**결정**: 옵션 A 채택.

**근거**:
- raw/nfi/ 폴더 자체가 `.gitignore` 3중 차단 (D8 + 5/24 커밋 a1cf786).
- csv 도 같은 폴더 → 같은 차단 적용.
- 외부 노출 위험 = raw xlsx 와 동일 (즉 0).
- 옵션 B 의 분석 정확도 손해보다 옵션 A 의 단순성·정확성이 우위.

**보안 보강**:
- *집계 결과만 git* 원칙 유지 (예: 수종별 평균 잔차, Weibull 모수).
- 좌표가 들어간 데이터프레임은 git 에 *절대 commit 금지* (raw/nfi/ 차단으로 자동 막힘).

### 결정 4: 단위 변환은 csv 시점에 적용

**결정**: 추출 시 D11 결정 5 의 단위 변환을 즉시 적용. csv 는 *분석 단위* 로 저장.

**적용**:
- 수고: cm → m (÷100). 컬럼명 `수고_m`.
- 지하고: cm → m (÷100). 컬럼명 `지하고_m`.
- 추정수고: cm → m (÷100). 컬럼명 `추정수고_m`.
- 흉고직경: cm 그대로. 컬럼명 `DBH_cm` (명시).
- 좌표·해발고·경사·재적: NFI 원본 단위 그대로.

**효과**: 분석 시 매번 ÷100 변환 안 함. 코드 단순화. 원자 단위 오류 차단.

### 결정 5: 출력 형식 — stand.csv + tree.csv 분리

**결정**: 표본점 단위 + 개별 나무 단위 분리. join key 는 '표본점번호'.

| 파일 | 행 수 | 크기 | 컬럼 |
|---|---|---|---|
| nfi7_chungbuk_stand.csv | 1,162 | 180KB | 24개 (집락·표본점·좌표·임상·영급·해발고·경사 등) |
| nfi7_chungbuk_tree.csv | 46,722 | 4.8MB | 18개 (표본점·수종·DBH·수고·재적·형질급 등) |

**근거**:
- climate_correct 는 *표본점 단위* (잔차 = 실측 V - lookup V) → stand.csv.
- Weibull fit 은 *개별 나무 DBH 분포* → tree.csv.
- 분리하면 분석에 맞춘 *부분 로딩* 가능.

### 보은 깊은 진단 결과 (nfi_probe.py v4)

추출본 csv 기반 0.5초 진단. 5가지 발견:

#### 발견 1: 우리 11 수종 매칭 — 보은 4,505 의 69% (3,107 그루)

| NFI 수종 | 가이드 매핑 | 본수 |
|---|---|---|
| 소나무 | 강원지방소나무 / 중부지방소나무 | 842 |
| 신갈나무 | 신갈나무 | 882 |
| 굴참나무 | 굴참나무 | 782 |
| 리기다소나무 | 리기다소나무 | 211 |
| 일본잎갈나무 | 낙엽송 | 176 |
| 상수리나무 | 상수리나무 | 135 |
| 잣나무 | 잣나무 | 70 |
| 곰솔 | 곰솔 | 9 |
| **합계** | | **3,107 (69%)** |

미매칭 1,398 그루 (31%) 중 상위:
- 졸참나무 253, 아까시나무 222, 잔털벚나무 215, 갈참나무 106, 신나무 90,
  밤나무 84, 굴피나무 79, 떡갈나무 45, 물오리나무 33, 물푸레나무 28.

**참나무류 통합 가능성**: 졸참·갈참·떡갈은 가이드 신갈·굴참·상수리와 동일 *참나무
속(Quercus)*. NFI 7차 보고서 표 C-6 도 "기타참나무류 (갈참·떡갈·졸참 등)" 으로
통합 코드 부여. 분석 시 *참나무 그룹 통합* 채택 권장 → 매칭 비율 약 78%.

#### 발견 2: 영급 × 임상 cross-tab — 핵심 5 그룹

| 영급 \\ 임상 | 침엽수림(D) | 혼효림(M) | 활엽수림(H) | 결측 |
|---|---|---|---|---|
| 1영급 | 0 | 0 | 4 | 0 |
| 3영급 | 0 | 0 | 5 | 0 |
| **4영급** | 2 | **15** | **16** | 0 |
| **5영급** | **10** | **13** | **11** | 0 |
| 6영급 | 1 | 3 | 2 | 0 |
| 7영급 | 1 | 1 | 1 | 0 |
| 8영급 | 2 | 2 | 0 | 0 |
| 결측 | 0 | 0 | 0 | 13 |

핵심 5 그룹 (Weibull fit 가능, ≥10 표본점):
- 4영급(31-40년) × 활엽수림: 16
- 4영급 × 혼효림: 15
- 5영급(41-50년) × 혼효림: 13
- 5영급 × 활엽수림: 11
- 5영급 × 침엽수림: 10
- 합계: 65 표본점 (102 의 64%)

각 그룹 × 평균 52 그루/표본점 = 그룹당 약 **600 그루** → Weibull 모수 fit
*매우 강건*. 가이드 §5.5 의 "수종·영급별 회귀" 진짜 가능.

#### 발견 3: 수고 단위 검증 (D11 결정 2 재확인)

- 유효 855/4505 (19.0%) — *수고 결측 81% 발견*.
- 범위: 3.7 ~ 30.3m. 평균 13.58m.
- 한국 임분 평균 12-16m 와 매칭. *단위 변환 ÷100 = m 검증 통과*.

**수고 결측 81% 의미** (D11 결정 4 의 연장):
- 6차 지침서 §4.4.5: "수고 및 지하고는 *표준목에서만* 조사".
- 보은 102 표본점 × 표준목 10본 ≈ 1,020 측정 가능. 실측 855 와 매칭.
- 정리: NFI 의 *수고*는 표본점당 표준목 5~10본 한정. 회귀 input 부적합.
- 추정수고 컬럼 활용 가능성 — 다음 진단 (D13) 에서 확인.

#### 발견 4: 형질급 분포

| 형질급 | 본수 | 비율 |
|---|---|---|
| 1급목 (통직, 부패 없음) | 1,450 | 32.2% |
| 2급목 | 2,135 | 47.4% |
| 3급목 (피압목 포함) | 920 | 20.4% |

**Weibull fit 대상 옵션**:
- 옵션 A: 1·2급목만 (3,585그루, 79.6%) — 양품 한정.
- 옵션 B: 전체 (4,505그루) — 통계 강건.
- 옵션 C: 형질급 별도 fit — 등급분포의 *기초*.

D14 (Weibull) 결정 시 채택. 잠정 추천: **옵션 B** (가이드 §5.5 의 "등급분포"
는 형질급 무관, *DBH 분포* 자체 fit).

#### 발견 5: 표본점당 본수 분포

- 매칭 86/102 표본점 (16 누락 — 비산림 또는 조사 불가).
- 본수 통계 (86 표본점):
  - 최소 3, 최대 127, 평균 52.4, 중앙값 47.
- 분포: 31-50그루 35표본점 (41%), 51-80그루 27표본점 (31%).
- 83/86 표본점이 ≥20 그루 → 표본점별 fit *기술적 가능*.

**의사결정**: 표본점별 fit *비추천*. 그룹 fit (5 핵심 그룹) 이 통계 강건성 ↑.

### D13/D14 설계 — 직접 입력 정리

D12 의 진단을 D13/D14 설계로 이어주는 핵심:

**D13 (climate_correct) 입력**:
- 표본점 단위 86개 (보은) → 잔차 회귀 input.
- 표본점별 V_actual: tree.csv 의 `추정간재적` 합산 / 0.04ha (기본조사원) = m³/ha.
- 표본점별 V_table: 우리 lookup_volume(우점수종, SI, 영급) — 보은 stand.csv.
- 잔차 = V_actual - V_table.
- 입력 변수: 해발고·경사·8방위·임상·영급 + 산악기상 4년 평균 (D10).
- 회귀: gradient boosting (LightGBM 또는 sklearn).
- 기대 R²: 0.5-0.7 (D11 결정 7).

**D14 (Weibull) 입력**:
- tree.csv (보은 4,505그루 또는 충북 46,722그루).
- 핵심 5 그룹 fit (영급 × 임상): 그룹당 약 600그루.
- 추가 수종별 fit: 신갈 882, 소나무 842, 굴참 782 — 각각 강건.
- 참나무 그룹 통합으로 매칭 78% 도달 가능.
- shape·scale 두 모수 fit (scipy.stats.weibull_min).

### 보안 결정 재확인

- raw xlsx 3개 (~340MB): `.gitignore` 차단 (D8 + a1cf786).
- 추출본 csv 2개 (5MB): 같은 폴더 → 같은 차단. 외부 노출 위험 0.
- 분석 산출물 (잔차 회귀, Weibull 모수): *집계 형태*로 git 가능.
- 절대 git X: 좌표 포함된 표본점 단위 데이터.

### 가이드 매칭

- §1.2 책임 4번(climate_correct) 입력 준비 완료 → D13 본격 설계.
- §1.2 책임 5번(Weibull) 입력 준비 완료 → D14 본격 설계.
- §3.1 NFI 데이터 명세 (좌표·임령·DBH·수고·재적) 모두 csv 에 매핑됨.
- §5.5 등급분포 회귀 (수종·영급별) 통계 강건성 확보 (그룹당 600그루).

### 후속 결정 예고

- **D13**: climate_correct 회귀 설계 (gradient boosting, 입력 변수, 검증).
- **D14**: 등급분포 Weibull fit 설계 (그룹 정의, 형질급 처리, 검증).

---

## D13: climate_correct() 회귀 설계 결정 (Day 6, 진행)

**일자**: 2026-05-27 (Day 6)
**책임**: 정우 (모듈 B/D)
**관련 가이드**: §2.3 임분수확표의 한계, §5.4 기후 보정 layer

### 배경

D11 (NFI 단위·구조) + D12 (csv 추출본·진단) 완료. 이제 가이드 §2.3·§5.4 의
climate_correct() 회귀 본 구현 직전. *알고리즘·하이퍼파라미터·인터페이스* 는
가이드 명시. 우리가 정해야 할 것은 *데이터 적용·결측 처리·검증* 의 8가지.

### 가이드 명시 (그대로 준수)

가이드 §5.4 L437-L453:
```python
from lightgbm import LGBMRegressor

nfi["v_table"] = nfi.apply(lambda r: lookup_yield(...), axis=1)
nfi["residual"] = nfi["volume_m3_per_ha"] - nfi["v_table"]

clim_feats = ["temp_anomaly_30y", "prcp_anomaly_30y", "vpd_max", "gdd_cum"]
clim_model = LGBMRegressor(n_estimators=200, max_depth=5, learning_rate=0.05)
clim_model.fit(nfi[clim_feats], nfi["residual"])
joblib.dump(clim_model, "data/processed/climate_correct.pkl")

def climate_correct(v_table, species, scenario, future_age):
    feats = load_scenario_features(scenario, future_age)
    residual = clim_model.predict([feats])[0]
    return v_table + residual
```

준수 사항:
- 알고리즘: **LightGBM** (LGBMRegressor)
- 하이퍼파라미터: **n_estimators=200, max_depth=5, learning_rate=0.05**
- Target: **residual = V_actual - V_table** (m³/ha)
- 출력: **data/processed/climate_correct.pkl**
- 호출: `climate_correct(v_table, species, scenario, future_age)`

---

### 결정 1: SI (지위지수) 추정 방법

**배경**: 가이드의 `lookup_yield(species, SI, age)` 는 SI 입력 필수. 그러나
NFI 7차 임황조사표에 SI 컬럼 *없음*. 추정 방법 결정 필요.

**옵션**:
- A. 표본점별 평균 수고 + 영급 → 수고곡선 역산 → SI 추정
- B. 표본점별 *우점수종 평균 수고* (NFI 표준목 수고 평균) → 수종별 표준 수고곡선 적용
- C. 수종별 *기본 SI* (전국 평균) 사용. 단순화.

**결정**: **옵션 B**.

**근거**:
- 표본점별 표준목 수고 평균 (보은 평균 13.58m) 측정값 활용.
- 임분수확표 (산림과학원 2014) 의 수종별 수고곡선 자료 사용 가능.
- 가이드 §3.2 lookup_volume 의 input 과 일관성.
- 옵션 A 는 자체 수고곡선 fit 필요 (작업 ↑).
- 옵션 C 는 표본점간 *기후 잔차* 추출 불가 (정의상 같은 V_table).

**구현 메모**:
- 영급 결측 13개 표본점 (D11 발견) → SI 추정 불가 → *해당 표본점 제외*.
- 수고 결측 81% — 표본점별 *유효 수고 평균* 으로 계산. 표본점당 평균 5~10본
  표준목 측정값 활용 (D11 결정 4).

### 결정 2: 기후 변수 4개 source — 정직한 한계 기록

가이드 §5.4 의 4개 변수 source:

| 변수 | 가이드 정의 | 우리 source | 가능 여부 |
|---|---|---|---|
| `temp_anomaly_30y` | 30년 평년 대비 기온 anomaly | 산악기상 4년(2022-2025) + 외부 평년 | 부분 가능 |
| `prcp_anomaly_30y` | 30년 평년 대비 강수 anomaly | **미수집** (D10 한계) | 불가능 |
| `vpd_max` | 연 최대 VPD | **미수집** (D10 산악기상에 VPD 컬럼 없음) | 불가능 |
| `gdd_cum` | 누적 GDD (생장일수) | 산악기상 일평균 기온 → 계산 | 가능 |

**결정**: 1차 회귀 = **2개 변수** (`temp_anomaly_30y`, `gdd_cum`) 만 사용.
2차 보강 = `prcp` 와 `vpd` 는 *NEX-GDDP-CMIP6* (GEE) 또는 *기상청 ASOS 평년값*
으로 보완 (D15+ 별도 결정).

**근거**:
- 가이드 §10.3: "강수는 부차적, 기온이 주축."
- 가이드 §10.3: "VPD 는 R² 5-10% 증가 — 있으면 좋지만 없어도 valid."
- 우리 실측 데이터로 시작 → R² 보고 보강 결정.

**정직한 한계**:
- 4개 변수 중 2개만 사용 → R² 0.5-0.7 (가이드 추정) 대비 *0.4-0.6* 으로 낮을 수 있음.
- 발표·논문에 *명시적 한계* 로 기록 — "1차 회귀는 2 변수, 보강 시 4 변수".

**대안 — Plan B**:
- 기상청 ASOS 보은·청주 평년값 (`30년 평년 1991-2020`) → 우리 산악기상과 차이.
- 단, ASOS = 저지대, 산악기상 = 산지 → 직접 비교 어려움. *별도 보정* 필요.

### 결정 3: 표본점 ↔ 산악기상 매칭 방법

**배경**: NFI 표본점 102개 (보은) vs 산악기상 관측소 6개 (D8). 각 표본점에
*어느 관측소* 의 기상 데이터를 매칭할지 결정.

**옵션**:
- A. 좌표 기반 최근접 (Euclidean distance)
- B. 좌표 + 해발고 보정 (3D 가중)
- C. 행정구역 기반 (보은군 = 보은 관측소 등)

**결정**: **옵션 A** (좌표 기반 최근접) — 단순, 정직.

**근거**:
- 보은 면적 ~590km², 산악기상 6 관측소 분포 = 평균 한 관측소가 100km² 담당.
  → 평균 매칭 거리 ~ 5-7km. *임지 단위 기후* 로 충분히 대표.
- 옵션 B 는 *지형도 + lapse rate* 필요. 작업 ↑, 정확도 미세 개선.
- 옵션 C 는 보은 = 6 관측소 모두 보은 → 의미 X.

**구현**:
- `scipy.spatial.cKDTree` 또는 `sklearn.neighbors.NearestNeighbors` 사용.
- NFI 표본점 좌표 (EPSG:5179) ↔ 산악기상 관측소 좌표 (EPSG:4326) → 통일 필요.
  → 산악기상을 EPSG:5179 (Korea 2000 / Central Belt) 로 변환.

**해발고 고려 (부차)**:
- 매칭 후 *해발고 차이* 를 회귀 input 에 추가 (`elev_diff` 변수).
- 표본점 96-678m × 관측소 242-627m → 차이 -300m ~ +400m 가능.

### 결정 4: V_actual 산출 방법

**배경**: NFI 의 표본점별 *실측 임목축적* (m³/ha) 계산 방법 결정.

**옵션**:
- A. tree.csv 의 표본점별 *추정간재적* 합산 / 0.04ha (기본조사원 면적)
- B. NFI 임분조사표의 *임목축적* 컬럼 직접 사용 (있다면)
- C. tree.csv DBH 단면적 합산 + 형수법 (BA × HF × 평균수고)

**결정**: **옵션 A** (추정간재적 합산).

**근거**:
- 추정간재적 = NFI 가 *수종·DBH·수고 기반* 으로 *이미 계산*한 개별 나무 재적.
- 형수법·V_table 회귀식보다 NFI 의 *공식 계산값* 신뢰성 ↑.
- 0.04ha 면적 보정으로 m³/ha 단위 통일.

**구현**:
```python
plot_actual = tree_df.groupby('표본점번호').agg(
    V_actual_m3_ha = ('추정간재적', lambda x: x.sum() / 0.04)
)
```

**짚을 점 — 대경목조사원**:
- NFI 부표본점 = 기본조사원(0.04ha, DBH 6-30cm) + 대경목조사원(0.08ha, DBH≥30cm)
- DBH 30cm 이상은 *0.08ha* 면적 적용 — 분리 처리 필요.
- 본 구현 시 코드 정확성 확인 필수.

### 결정 5: 검증 방법

**옵션**:
- A. K-fold cross-validation (5-fold)
- B. Train/test split (80/20)
- C. 지역 hold-out (충북 일부 군 → test)
- D. 시기 hold-out (5차/6차 → train, 7차 → test) — 5·6차 추출 필요

**결정**: **옵션 A (5-fold CV)** + **옵션 B (hold-out 80/20)** 병행.

**근거**:
- 5-fold CV → R² 평균·표준편차 → 일반화 성능 평가.
- Hold-out → 마지막 단일 평가 (논문 보고용).
- 옵션 C 는 지역 일반화 검증 — *추후 보강* 단계 (D15+).
- 옵션 D 는 시계열 변화 검증 — *추후 5·6차 추출 후* 가능.

**구현**:
```python
from sklearn.model_selection import KFold
from sklearn.metrics import r2_score, mean_squared_error

kf = KFold(n_splits=5, shuffle=True, random_state=42)
r2_scores = []
for fold, (train_idx, val_idx) in enumerate(kf.split(X)):
    model.fit(X.iloc[train_idx], y.iloc[train_idx])
    pred = model.predict(X.iloc[val_idx])
    r2_scores.append(r2_score(y.iloc[val_idx], pred))
```

### 결정 6: 결측 처리

**문제 데이터**:
- 영급 결측: 13 표본점 (D11 발견) → SI 추정 불가
- 수고 결측: 81% (D11·D12 발견) → 표본점 평균 수고는 표준목 한정
- 기후 결측: 산악기상 18일 1.1% (D10 발견)

**결정**:
- **영급 결측 표본점**: 분석에서 *완전 제외*. SI 추정 불가 → V_table 산출 불가.
- **수고 결측**: 표본점별 *유효 수고 평균* 계산. 표준목 5-10본 평균값으로
  대표. (대체 X — 정직한 측정값만 사용)
- **기후 결측 18일**: 산악기상 월/연 통계는 이미 보정됨 (D10). 회귀 input 영향 X.
- **추정간재적 결측**: 해당 나무 *제외*. 표본점 V_actual 산출 시 누락.

**기대 영향**:
- 보은 86 매칭 표본점 (D12 발견 5) 중 영급 결측 13 제외 → *73 표본점* 유효.
- 가이드 §3.4 의 27-30 표본점 추정의 *2.4배* — 회귀 강건성 유지.

### 결정 7: 수종 매핑 — D12 결정 활용 + 참나무 통합

**결정**: 표본점 우점수종을 우리 11 수종으로 매핑. 참나무 그룹 통합.

**우점수종 정의**: 표본점 내 *개별 나무 중 본수 최다 수종*.

**매핑 표** (D12 발견 1 보강):

| NFI 수종 | 가이드 매핑 | 처리 |
|---|---|---|
| 소나무 | 강원지방소나무 / 중부지방소나무 | 충북 = 중부지방소나무 |
| 잣나무 | 잣나무 | 그대로 |
| 리기다소나무 | 리기다소나무 | 그대로 |
| 일본잎갈나무 | 낙엽송 | 그대로 |
| 곰솔 | 곰솔 | 그대로 |
| 신갈나무 | 신갈나무 | 그대로 |
| 굴참나무 | 굴참나무 | 그대로 |
| 상수리나무 | 상수리나무 | 그대로 |
| **졸참나무·갈참나무·떡갈나무** | **참나무 그룹** | **신갈나무 표준 적용** |
| 기타 (아까시·잔털벚 등) | 매핑 X | *분석 제외* |

**근거**:
- 참나무류는 가이드 §1.2 우선순위 활엽수에 포함.
- 동일 *참나무속* — 수고곡선·재적 비슷.
- D12 발견 1: 참나무 통합 시 매칭 78% (그렇지 않으면 69%).

**예상 분석 대상**:
- 보은 102 표본점 중 우점수종 매핑 가능 (참나무 통합) ≈ *85-90 표본점*.
- D6 의 결측 13 제외 → 약 *70-75 표본점* 최종 회귀 input.

### 결정 8: 분석 범위 — 보은 우선, 충북 fallback

**결정**:
- **1차 회귀**: 보은 *70-75 표본점* (위 결정 6·7 적용).
- **2차 검증**: 충북 전체 *800-900 표본점* (영급 결측·미매칭 수종 제외).

**근거**:
- 모듈 A (민석) 가 보은 학습 → 우리도 보은 *일치* (모델 호환성).
- 보은 70-75 → 가이드 §3.4 추정의 2.4배 — 단독 회귀 강건성 OK.
- 충북 800+ → 일반화 성능 검증 + 보은 회귀 결과 비교.
- 발표 시: "보은 모델 학습 + 충북으로 일반화 검증" 의 *학술적 강점*.

### 후속 결정 예고

- **D14**: 등급분포 Weibull fit 설계 (그룹 정의, 형질급 처리, 검증).
- **D15**: 기후 변수 4개 보강 (NEX-GDDP-CMIP6 또는 ASOS 평년).
- **D16**: 5·6차 시계열 분석 (영급 변화, 잔차 시간 추세).

### 가이드 매칭

- §2.3 임분수확표의 한계: 기후 보정 layer 필요성 인정 + 구현 결정.
- §5.4 climate_correct: 알고리즘·하이퍼파라미터·인터페이스 *100% 준수*.
- §10.3 강수·VPD 부차성: 1차 회귀 2 변수, 2차 보강 정직 기록.
- §3.4 보은 표본점 추정 27-30: 우리 실측 70-75 (영급 결측 제외) — 2.4배 자산.

### 다음 본 구현 작업

1. `module_bd/src/lookup_yield.py` (또는 lookup_volume.py 의 SI 입력 추가)
2. `module_bd/src/climate_correct.py`:
   - SI 추정 함수
   - V_actual 산출 (대경목조사원 분리)
   - 표본점 ↔ 산악기상 좌표 매칭
   - 기후 변수 2개 계산 (temp_anomaly, gdd_cum)
   - LightGBM fit + 5-fold CV
   - climate_correct.pkl 저장 + climate_correct() 함수
3. `module_bd/data/processed/climate_correct.pkl` 산출
4. `module_bd/tests/test_climate_correct.py` 단위 테스트 5-10개

---

### D13 사후 보강 (2026-05-27 저녁)

**진행 상태: (Day 6, 진행) → (Day 6, 완료)**

#### 실측 5 시도 진전

D13 결정 8 의 "표본점 ~70-75 예상" 정도가 *적었음* 발견. 다양한 변수·구조 조합 시도:

| 시도 | 입력 데이터 | 변수 | 표본 | R² | 정직한 한계 |
|---|---|---|---|---|---|
| v1 | 보은 7차 + 산악기상 4년 | 3 | 68 | **-0.013** | 시기 미스매치 (2022-25 산악기상 vs 2016-20 NFI) |
| v2 | + ASOS 30y 평년 (보은 1개) | 6 | 68 | **-0.029** | 4 anomaly 상수 → LightGBM 학습 신호 0 |
| v3 | + 임상 (D/H/M) | 7 | 65 | **-0.039** | 변수 추가만으로 한계, 과적합 |
| v4 | 충북 + ASOS 5 시군 매칭 | 6 | 755 | **+0.027** | 첫 양수, 공간 변동 작음 |
| **v5** | **+ NFI 6차 시계열 패널** | **6** | **1369** | **+0.204** | **시간 변동 학습 성공 (best)** |

#### 결정적 통찰 (사용자 짚음)

1. *시기 미스매치 의심* → ASOS 30년 평년 확보 (시도 2-3)
2. *충북 확장 가치 평가* → 5 시군 ASOS (시도 4)
3. *NFI 시기 맞춤 핵심* → 6+7차 시계열 패널 (시도 5, 큰 진전)
4. *B 분리 코드 정직성* → climate_features_panel.py + fit_correct.py 분리 유지

#### v5 변수 중요도

| 변수 | 중요도 | 비고 |
|---|---|---|
| elev | 51.4% | 공간 변수 (해발고), 압도적 |
| prcp_anomaly_30y | 14.0% | 강수 anomaly |
| vpd_anomaly | 12.6% | 수증기 부족 anomaly |
| temp_anomaly_30y | 9.9% | 기온 anomaly |
| imsang_code | 6.3% | 임상 (D/H/M) |
| gdd_anomaly | 5.8% | GDD 누적 anomaly |

기후 4 변수 합계: 42.3% (v4 의 18.3% → 2.3배 증가)

#### 정직한 한계 (잔차 79.6% 가 모델 외)

- *임분 우연성*: 특정 표본점의 강한 나무 1-2개
- *측정 오차*: 표준목 5-10본 평균의 표본 오차
- *NFI 5차 미통합*: 영문 코드 + Bessel 좌표 변환 필요 (다음 작업)
- *V_table 시간 고정*: 임분수확표 2014 정적 (6차 7차 둘 다 같은 V_table)

#### v6 ablation 결과 (실패 시도, 정직한 기록)

- 시도: 이상치 제거 (1%/99% 분위수) + 수종 세분화 (3 그룹 → 7 수종)
- 결과: R² 0.204 → 0.147 (-0.057, 역효과)
- 학습:
  - 이상치 제거 = 진짜 신호 (낙엽송 +146 등) 손실
  - 7 수종 = 일부 표본 부족 (낙엽송 92, 잣 55) → 과적합
- 결정: v5 = best 모델 유지

#### 가이드 비교

- 가이드 §10.3: "Module B 는 baseline scenario (lookup만, 기후 보정 제외) 도 valid"
- 우리 R² 0.204 = baseline 보다 *유의미한 진전*
- 가이드 §5.4 R² 0.5-0.7 추정 대비 낮으나, 가이드 = NEX-GDDP + SHAP 더 정교, 우리 = ASOS + LightGBM 단순화

#### 산출물

- `module_bd/data/processed/climate_correct.pkl` (best 모델, 261 KB)
- `module_bd/data/processed/asos_anomaly_panel.csv` (45 행 시기별 anomaly 패널)
- `module_bd/src/climate_correct/fit_correct.py` (v5 회귀)
- `module_bd/src/climate_correct/climate_features_panel.py` (시기별 anomaly 산출)
- `module_bd/src/climate_correct/asos_features.py` (보은 평년 산출)
- `module_bd/src/climate_correct/asos_chungbuk_features.py` (5 시군 평년)
- `module_bd/src/asos_collect.py` + `asos_chungbuk_collect.py` (수집 인프라)
- ASOS 5 시군 1991-2020 일자료 (54,790 행, ~55MB)

#### 다음

- D14 (Weibull 등급분포) 진행
- 향후 R² 개선 가능: NFI 5차 통합 (영문 매핑 + Bessel 변환 작업)

---

## D14: 등급분포 Weibull fit 설계 결정 (Day 7, 완료)

**일자**: 2026-05-28 (Day 7)
**책임**: 정우 (모듈 B/D)
**관련 가이드**: §5.5 등급분포, §8.1 GrowthForecast.grade_distribution_trajectory

### 배경

D11 (NFI 단위) + D12 (csv 추출·진단) 완료. growth_predict() 는 임분의
*평균* volume/dbh/height 만 출력. 그러나 실제 임분은 큰·중간·작은 나무 혼합.
정확한 *원목 등급별 매출* (Faustmann NPV) 을 위해 DBH 분포 필요.

가이드 §5.5: "수종·영급별 등급분포 (Weibull) fit".
가이드 §8.1: `GrowthForecast.grade_distribution_trajectory` (Optional 필드).

### 진단 결과 (weibull_probe.py)

- 충북 46,722 그루, DBH 6-78cm, 평균 16.8cm.
- 히스토그램: 역-J 분포 (8-12cm 피크 11,179, 오른쪽 꼬리).
- **전체 왜도 +1.112** → 오른쪽 꼬리 분포. Weibull 교과서적 적합.
- 영급 × 임상 23 그룹 (≥100 그루), 영급 fallback 7 그룹.

### 결정 1: 분포 모델 — Weibull min (loc 고정 6cm)

**결정**: `scipy.stats.weibull_min`, loc=6cm 고정 (floc).

**근거**:
- Weibull = 임분 직경분포 표준 모델 (학술 문헌 다수).
- 2 모수 (shape c, scale λ) — 간결, 해석 가능.
- loc=6 고정: NFI 기본조사원 최소 측정 DBH 6cm = 분포 시작점.
  loc 자유 fit 시 음수 가능 (비물리적) → 고정이 안전.

**대안**:
- 정규분포: 왜도 +1.112 무시 → 부적합.
- 로그정규: 가능하나 Weibull 이 임업 표준.
- 감마: Weibull 과 유사, 임업 관례상 Weibull.

### 결정 2: 그룹 단위 — 영급 × 임상 + 영급 fallback

**결정**: 영급 × 임상 (23 그룹) 우선, 없으면 영급 (7 그룹) fallback,
그래도 없으면 가장 가까운 영급.

**근거**:
- 가이드 §5.5 "수종·영급별" + 우리 임상 (D/H/M) 추가 = 더 세밀.
- D12 발견: 그룹당 수백~수천 그루 → 강건 fit.
- 수종별 (신갈 882, 소나무 842) 도 가능하나, climate_correct v6 의
  수종 세분화 실패 경험 → 영급 × 임상 이 더 안정적 (표본 충분).
- fallback 계층: 그룹 → 영급 → nearest 영급 (항상 답 보장).

**대안 (수종별)**: 추후 보강 가능. 현재 미채택.

### 결정 3: 형질급 처리 — 무관 (DBH 분포 자체)

**결정**: 형질급 (1/2/3급) 무관. 전체 그루 DBH 분포 fit.

**근거**:
- D12 발견 4 잠정 추천 옵션 B: 등급분포는 형질급 무관, DBH 분포 자체.
- 원목 *등급* = DBH 크기 (소경/중경/대경) → 형질급과 별개 개념.
- 형질급 = 통직·부패 등 품질 → 추후 가격 보정에 별도 활용 가능.

### 결정 4: DBH 등급 구분 — 소경/중경/대경

**결정**:
- 소경재: 6-18cm
- 중경재: 18-30cm
- 대경재: 30cm+

**근거**: 한국 원목 시장 통용 구분. KOFPI 가격 등급과 연계 가능.
NFI 경급 코드 (D11 결정 6: 소경 6-18, 중경 18-30, 대경 30+) 와 일치.

### 결정 5: 검증 — KS 검정 + 등급 비율 단조성

**결정**: Kolmogorov-Smirnov 검정 (적합도) + 등급 비율 물리 검증 병행.

**KS 결과 (정직한 한계)**:
- KS 적합 (p>0.05): **2/23 그룹 (9%)**.
- 대부분 그룹 p<0.05 (기각).

**정직한 해석 — KS 낮음이 실패 아닌 이유**:
1. KS 검정은 대표본 (n>1000) 에서 *극도로 엄격*. 미세 편차도 기각.
   - 4영급 활엽수 9,230 그루 → p=0.000 (기각).
   - 7영급 혼효림 136 그루 → p=0.436 (통과). 작은 표본만 통과.
2. shape ~1.0-1.6 = 전형적 임분 Weibull (문헌 일치).
3. **등급 비율 단조 증가 = 모수 유효성 진짜 증거**:
   - 대경재 비율: 2영급 0.1% → 4영급 6.8% → 6영급 17.0% → 8영급 17.8%.
   - 영급 ↑ → 대경재 ↑ (생장 물리 법칙 완벽 매칭).

**결론**: KS p 낮으나 모수는 등급 비율 산출에 유효. 학술 정직성으로
KS 한계 명시 + 물리 검증으로 유효성 별도 입증.

### 결정 6: grade_distribution() 함수 인터페이스

**결정**:
```python
grade_distribution(age_class, imsang=None, n_total_per_ha=None)
# 반환: {소경재, 중경재, 대경재 (본수), proportions, shape, scale, fallback}

grade_distribution_trajectory(age_class_now, imsang, n_per_ha_trajectory, forecast_years)
# growth_predict() trajectory 통합용
```

**근거**:
- 가이드 §8.1 `grade_distribution_trajectory` 필드 채움.
- 단일 (특정 시점) + trajectory (시계열) 둘 다 지원.
- 비율 정규화 (CDF 차이 합으로 나눔) → 본수 합 ≈ 입력.

**trajectory 검증 (물리적 정상)**:
- 4영급 시작 → 30년: 본수 감소 (1200→550, 자연 도태) + 대경재 비율 증가.
- 20→30년 대경재 절대 본수 감소 (129→91): 비율↑ 하나 전체 본수↓ 가 큼.
  노령림 = 큰 나무 있으나 그루 적음. NPV 는 재적으로 보상.

### 산출물

- `module_bd/src/diagnose/weibull_probe.py` (DBH 분포 진단)
- `module_bd/src/weibull_fit.py` (Weibull fit → json)
- `module_bd/src/grade_distribution.py` (등급별 본수 예측 함수)
- `module_bd/data/processed/weibull_params.json` (23 그룹 + 7 fallback 모수)
- `module_bd/tests/test_grade_distribution.py` (14 테스트 통과)

### 한계

- KS 적합률 9% (대표본 엄격) — 모수는 등급 비율로 유효성 입증.
- 수종별 fit 미구현 (영급 × 임상 우선). 추후 보강 가능.
- 영급 추정 단순 (10년마다 +1영급) — 실제 생장률 변동 미반영.
- NFI 7차 단일 시점 — 시계열 (5/6차) 등급분포 변화 미분석.
- growth_predict() 통합 미완 (함수는 준비, 호출 연결은 다음 작업).

### 가이드 매칭

- §5.5 등급분포 Weibull fit *구현 완료*.
- §8.1 `grade_distribution_trajectory` 채움 (함수 준비).
- §3.2 lookup_volume (DBH → 재적) 과 연계 가능.

### 다음 작업과 연결

- growth_predict() 에 grade_distribution_trajectory 통합 (n_per_ha 활용).
- 희도 모듈 C: 등급별 본수 × 등급별 재적 × 등급별 가격 = 원목 매출.
- (선택) 시각화: 히스토그램 + Weibull 곡선 (발표 Figure).

### 후속 결정 예고

- **D15**: 기후 변수 4개 보강 (NEX-GDDP-CMIP6 또는 ASOS 평년) — R² 개선.
- **D16**: NFI 5·6차 시계열 분석 (영급 변화, 잔차 시간 추세, 등급분포 변화).

---

## D16: NFI 5차 통합 — 시계열 3시점 확장 (Day 7, 완료)

**일자**: 2026-05-28 (Day 7)
**책임**: 정우 (모듈 B/D)
**관련 가이드**: §5.4 climate_correct, §3.1 NFI 데이터

### 배경

D13 사후보강에서 v5 (NFI 6+7차 패널, R² 0.204) 달성. "향후 R² 개선:
NFI 5차 통합" 예고. 5차 통합 시 시계열 시점 2 → 3 (2008/2012.5/2018),
시간 span 확대로 anomaly 변동 증가 기대. D13 에서 우려한 "영문 코드 +
Bessel 좌표 변환"의 실제 난이도를 진단으로 확정.

### 진단 결과 — 5차 통합 장벽 *모두 해소* (큰 발견)

5차 xlsx 구조 진단 (nfi_5_6_7_probe + nfi5_coord_probe):

| 항목 | 우려 (D13) | 실측 진단 | 결론 |
|---|---|---|---|
| 컬럼명 | 영문 코드 | GYEAR, SP_ID, GF_CODE_*_NM 등 | 한글 매핑만 |
| 좌표 | Bessel 변환 필요 | 충북 Y 283666~415745, X 227884~343976 | **7차와 동일 범위 (EPSG:5181, 변환 불필요)** |
| 표본점 ID | 불일치 우려 | 5차·7차 직접 공통 **1078개** | 고정표본점 동일 |
| 임상 코드 | 다를 수 있음 | '침엽수림(D)', '활엽수림(H)', '혼효림(M)' | 6/7차와 동일 |
| 영급 코드 | 다를 수 있음 | '1영급'~'9영급' | 6/7차와 동일 |
| 측정연도 | 불명 | 2006~2010 균등 (247/224/217/202/225) | 시점 3 확보 |
| 수고 단위 | 불명 | 340~2590 (cm), 유효 21.2% | ÷100 = m (6/7차 동일) |

**핵심**: "Bessel" 라벨이나 좌표값은 EPSG:5181 동일 범위 → 변환 불필요.
좌표·ID·임상·영급 코드 모두 6/7차와 동일 → 영문→한글 매핑 + 수고 cm→m
변환만으로 통합 가능. 예상 난이도(3-4시간)보다 훨씬 단순.

### 결정 1: 5차 추출 — 6/7차 호환 컬럼명으로 저장

**결정**: 영문 컬럼 → 한글 (6/7차 동일 컬럼명) 매핑. 수고/추정수고 cm→m
변환 후 저장. fit_correct.py 가 차수 구분 없이 동일 처리 가능.

**산출**: nfi5_chungbuk_stand.csv (1115 표본점, 141KB),
nfi5_chungbuk_tree.csv (52807 그루, 3218KB). raw/nfi/ gitignore 차단.

### 결정 2: ASOS anomaly panel 확장 — 2006-2010 추가

**결정**: NFI_MEASURE_YEARS 에 2006-2010 추가 (9→14년). 평년은 동일
1991-2020. ASOS 일자료가 이미 1991-2020 보유 → 추가 수집 불필요.

**산출**: asos_anomaly_panel.csv 45행 → 70행 (5 시군 × 14년, 결측 0).

**검증 — 시간 변동 (온난화 신호)**:
- 보은: 5차(2008) +0.200°C → 6차(2012.5) -0.162°C → 7차(2018) +0.675°C.
- 5개 시군 모두 5차 < 7차 (온난화 추세). 6차가 상대적 골짜기 (U자형).
- temp_anom std: 0.494 (9년) → 0.439 (14년). 약간 감소하나 표본 수 ↑.

### 결정 3: v7 채택 — R² 동일하나 안정성 2배

**결과**:

| 버전 | 데이터 | 표본 | R² | std |
|---|---|---|---|---|
| v5 | 6+7차 (시점 2) | 1369 | 0.204 | ±0.074 |
| **v7** | **5+6+7차 (시점 3)** | **2194** | **0.204** | **±0.038** |

**결정**: v7 채택. R² 동일(0.204)하나 **fold std 절반 (0.074→0.038)**,
표본 60% 증가 (1369→2194). 더 많은 데이터로 같은 설명력 = 일반화 강건.

**정직한 분석 — R² 안 오른 이유**:
- 잔차 79.6%가 모델 외 (임분 우연성). elev 47% 공간 지배.
- V_table 정적(2014) → 시간 보정 한계. R² 천장 ~0.2 (현 변수).
- 5차 추가는 안정성·시계열 폭에 기여, R² 절대값엔 한계.

**핵심 발견 — 차수별 잔차 단조 증가 (학술 가치)**:
- 5차(2008) -6.1 → 6차(2012.5) -0.2 → 7차(2018) +11.7 (m³/ha).
- 시간 ↑ → 잔차 ↑ (단조). "임분수확표 2014 대비 최근 임분축적 증가"
  = 온난화/생장환경 변화 또는 임분 성숙 신호. 시계열 3시점으로 실증.

### 산출물

- `module_bd/src/diagnose/nfi5_coord_probe.py` (좌표 체계 진단)
- `module_bd/src/diagnose/nfi5_extract.py` (영문→한글 + 수고 cm→m)
- `module_bd/data/raw/nfi/nfi5_chungbuk_{stand,tree}.csv` (gitignore)
- `module_bd/data/processed/asos_anomaly_panel.csv` (70행)

### 한계

- temp_anom std 약간 감소 (0.494→0.439) — 6차 골짜기가 분산 희석.
- R² 절대값 미개선 (0.204 유지) — 천장은 V_table 정적성·임분 우연성.
- 5차 수고 유효 21.2% (6/7차 표준목 패턴 동일) — SI 추정 표본 한정.

### 가이드 매칭

- §5.4 climate_correct 시계열 입력 확장 (시점 2 → 3).
- §3.1 NFI 데이터 (5/6/7차 통합) 완성.

### 후속

- D17: SI 변수 추가 (R² 개선 시도).

---

## D17: SI (지위지수) 변수 추가 + measure_year ablation (Day 7, 완료)

**일자**: 2026-05-28 (Day 7)
**책임**: 정우 (모듈 B/D)
**관련 가이드**: §5.4 climate_correct

### 배경

D16 (v7, R² 0.204, 시점 3) 후 "R² 더 끌어올리기" 진행. 정직한
ablation (한 번에 변수 하나씩, v6 복합변경 실패 교훈) 으로 입지 변수
탐색. measure_year 실패도 정직히 기록.

### 결정 1: SI (지위지수) 변수 추가 — 채택

**배경**: estimate_si() 가 V_table lookup 용으로 SI 를 이미 계산하나
회귀 feature 로는 미사용 (버려짐). SI = 입지 생산력 → 잔차 설명 가능성.

**결과**:

| 버전 | 변수 | R² | SI 중요도 |
|---|---|---|---|
| v7 | 6 (anomaly 4 + elev + imsang) | 0.204 | — |
| **v8** | **7 (+ si)** | **0.228** | **8.5%** |

**결정**: SI 채택. R² +0.024 (12% 상대 향상). SI 중요도 8.5% =
elev (42.7%) 와 별개 정보 (elev=고도 간접, si=생산력 직접). 시간 불변
변수라 미래 예측(climate_scenario)에도 유효.

**근거**:
- 이미 계산되던 값 재활용 (rows 에 'si' 추가 + FEATURES 추가, 최소 수정).
- v3 (변수 추가만으로 하락) 과 달리, SI 는 물리적으로 타당한 입지 변수.
- 미래 예측 시 elev/si/imsang = 임분 고유값으로 활용 (D15 연결).

### 결정 2: measure_year ablation — 효과 없음, 제거 (정직한 기록)

**배경**: 차수별 잔차 단조 증가 (5차 -6.1 → 7차 +11.7, D16) 가 명확해
measure_year 직접 변수화 시 R² 크게 오를 것으로 기대.

**결과**:

| 버전 | 변수 | R² | measure_year 중요도 |
|---|---|---|---|
| v8 | 7 | 0.228 | — |
| v9 | 8 (+ measure_year) | 0.223 | 4.5% |

**결정**: measure_year **제거**, v8 유지. R² -0.005 (효과 없음).

**정직한 해석 (중요)**:
- measure_year 추가했는데 R² 오히려 하락 + 중요도 낮음 (4.5%).
- **원인: anomaly 변수가 이미 시간 trend 흡수**. measure_year 의 시간
  신호 = anomaly 와 중복 → 정보 안 늘고 노이즈만 증가 (약간 과적합).
- **이게 오히려 기후 변수 정당성 입증**: "시간 때문"이 아니라
  "기후(anomaly) 때문"에 잔차가 변함 → climate_correct 의 학술적 정당성.
- measure_year 가 효과 컸다면 "시간만 외운" 의심 → 효과 없음이 더 정직.

### 최종 R² 발전 (전체)

| 버전 | 구성 | R² | 비고 |
|---|---|---|---|
| v1 | 보은 산악기상 | -0.013 | 시기 미스매치 |
| v2 | + ASOS 30y 상수 | -0.029 | anomaly 상수 |
| v3 | + 임상 | -0.039 | 변수 추가만 |
| v4 | 충북 5 시군 | +0.027 | 첫 양수 |
| v5 | 6+7차 패널 | +0.204 | 시간 변동 학습 |
| v7 | 5+6+7차 패널 | +0.204 | 시점 3, std 절반 |
| **v8** | **+ SI** | **+0.228** | **best** |
| v9 | + measure_year | +0.223 | 효과 없음, 제거 |

### v8 변수 중요도

| 변수 | 중요도 |
|---|---|
| elev | 42.7% |
| prcp_anomaly_30y | 11.7% |
| gdd_anomaly | 11.3% |
| vpd_anomaly | 10.8% |
| temp_anomaly_30y | 9.7% |
| si | 8.5% |
| imsang_code | 5.4% |

### 산출물

- `module_bd/src/climate_correct/fit_correct.py` (v8, 7변수)
- `module_bd/data/processed/climate_correct.pkl` (v8, 327KB)

### 한계

- R² 0.228 = 잔차의 23%만 설명. 77%는 임분 우연성·V_table 정적성·
  SI 추정 오차·우점수종 단순화 (모델 외).
- R² 천장 ~0.23 (현 변수 구조). 추가 변수 효과 제한적 (measure_year 실증).

### 가이드 매칭

- §5.4 climate_correct: SI 변수로 입지 정보 보강.
- §10.3 baseline 도 valid → 우리 R² 0.228 = 유의미한 진전.

### 후속

- D15: NEX-GDDP SSP 시나리오로 growth_predict 통합.

---

## D15: 기후 보정 통합 — NEX-GDDP SSP + 외삽 정직 감지 (Day 7, 완료)

**일자**: 2026-05-28 (Day 7)
**책임**: 정우 (모듈 B/D)
**관련 가이드**: §5.4 climate_correct, §3.1 SSP 시나리오, §8.2 growth_predict

### 배경

D13~D17 로 climate_correct.pkl (v8, R² 0.228) 완성. 그러나 growth_predict()
의 climate_scenario 는 baseline 외 미작동 (경고만). 가이드 §5.4 의
climate_correct(v_table, scenario, ...) = V_table + residual 을 진짜 구현.
"민석(모듈 A)에게 받지 말고 필요하면 우리가 한다" → NEX-GDDP 직접 추출.

### 결정 1: 미래 기후 source — NEX-GDDP-CMIP6 직접 추출 (GEE)

**결정**: GEE JavaScript Code Editor 로 NASA/GDDP-CMIP6 직접 추출.

**파라미터** (가이드 §3.1 + 판단):
- 모델: 5개 GCM 앙상블 평균 (ACCESS-CM2, MPI-ESM1-2-HR, MRI-ESM2-0,
  NorESM2-MM, UKESM1-0-LL). 가이드 미지정 → IPCC 식 앙상블 (단일 모델
  불확실성 회피).
- 기간: 2021-2050 (근미래). 가이드 2021-2100 중 벌기령 의사결정 구간.
- 시나리오: ssp245, ssp585 (NEX 제공분; ssp126 은 GEE 자산 미제공).
  가이드 SSP1-2.6/2-4.5/5-8.5 중 245/585 충족.
- anomaly: 미래(2021-2050) - NEX historical(1991-2020). 모델 내 차이로
  GCM bias 상쇄.

**변수 (가이드 §5.4 4개 정확히)**:
- temp_anomaly_30y ← tas (K→°C)
- prcp_anomaly_30y ← pr (kg/m²/s → mm/day)
- gdd_anomaly ← tas 일평균 GDD(base 5°C) 누적
- vpd_anomaly ← tas + hurs (상대습도) → VPD = es(1-RH/100), 연 최대

**산출**: nex_gddp_chungbuk_anomaly.csv (5 시군 × 2 시나리오 = 10행).
검증: 청주 ssp245 temp +1.39°C, ssp585 +1.65°C (IPCC 한반도 전망 일치,
ssp585 > ssp245 모든 변수). processed/nex_scenario_anomaly.csv 로 복사.

### 결정 2: growth_predict 통합 — elev/sigun 인자 추가

**결정**: growth_predict() 에 elev (해발고), sigun (시군, 기본 '보은')
인자 추가. climate_correct.pkl 의 7 feature 중 기후 4개는 NEX, 입지
3개(elev/imsang/si)는 임분 고유값으로 구성.

**보정 적용**: V_corrected = V_table + residual (climate_correct 학습 방식).
residual 은 기후+입지만 (나이 무관) → 모든 trajectory 시점에 동일 보정.
trajectory 에 volume(baseline) + volume_corrected + climate_residual 병기
(산주가 기후 영향 비교 가능).

### 결정 3: 외삽 정직 감지 (방법 A) — 핵심 결정

**문제 발견**: SSP245 (+72.8) = SSP585 (+72.8) 동일 보정. 원인 진단:

| 변수 | 학습(ASOS) 범위 | NEX 미래 | 외삽? |
|---|---|---|---|
| temp | -0.675 ~ +1.159 | +1.35 ~ +1.68 | **외삽 (전부 밖)** |
| prcp | -516 ~ +871 | +68 ~ +118 | 내삽 |
| gdd | -141 ~ +375 | +339 ~ +441 | 부분 외삽 |
| vpd | -0.38 ~ +0.77 | +0.08 ~ +0.13 | 내삽 |

**원인**: 미래 기온(NEX)이 과거 학습(ASOS) 최대 범위를 넘음 (온난화 →
필연적). LightGBM(트리)은 외삽 구간에서 상수 출력 → SSP245/585 둘 다
"최대 온도 잎" → 동일 보정. 트리 모델의 본질적 한계.

**대안 비교**:

| 방법 | 내용 | 정직성 | 채택 |
|---|---|---|---|
| **A** | **외삽 감지 + 경고 + 방향성 참고 명시** | **최상** | ✓ |
| B | 선형 성분으로 SSP 구분 | 중 (구분도 외삽 가정) | |
| C | 미래 예측 철회, baseline만 | 상 (D15 포기) | |
| D | anomaly 정규화 (스케일) | 하 (편법) | |

**결정**: 방법 A. 보정값은 제공하되, 외삽을 코드가 자동 감지하여 경고.
"보정값은 방향성 참고용, 정확한 크기 불확실, SSP 시나리오 구분 제한적".

**근거**:
- 외삽은 모든 미래 기후 영향 예측의 본질적 한계 (온난화 = 과거 범위 초과).
- 트리 모델 외삽 부적합 = 학계 정설.
- "SSP245=585"를 그럴듯한 숫자로 숨기는 것이 가장 부정직.
- 방법 B (선형)도 그 구분이 외삽 가정이라 과신 위험 — 더 정교해 보일 뿐.
- 한계를 정확히 드러내는 것이 학술적으로 가장 떳떳 (심사 신뢰).

**구현**:
- `_load_train_anomaly_ranges()`: asos_anomaly_panel.csv 에서 변수별
  학습 범위 런타임 계산 (학습 데이터 = 진실의 원천, pkl 재학습 불필요).
- `_compute_climate_correction()`: NEX anomaly vs 학습 범위 변수별 비교.
  외삽이면 변수별 명시 경고 + climate_extrapolation 플래그.
- trajectory['climate_extrapolation'] = True/False (모듈 C/E 가 인지).

### 산출물

- GEE 추출 스크립트 (NEX-GDDP, 사용자 환경)
- `module_bd/data/raw/nex/nex_gddp_chungbuk_anomaly.csv`
- `module_bd/data/processed/nex_scenario_anomaly.csv`
- `module_bd/src/growth_predict.py` (D15 통합: elev/sigun + 보정 + 외삽 감지)
- 테스트 59 passed (기존 영향 0)

### 한계 (정직히)

- **미래 기온 외삽**: NEX temp(+1.35~1.68) > 학습 ASOS(+1.16). 트리 모델
  외삽 → SSP245=SSP585 동일 보정(+72.8). 시나리오 구분 데이터 한계.
- **보정값 크기 불확실**: +72.8 = 학습 범위 끝 잎의 잔차값. 방향성(온난화
  → V 증가 경향)은 의미, 정확한 크기는 외삽이라 신뢰 낮음.
- **나이 무관 보정**: climate_correct 는 나이 변수 없음 → 모든 시점 동일
  보정. 실제 나이별 기후 영향 차이 단순화.
- **해결 방향 (향후)**: 더 긴 시계열 (4/5/6/7차) 또는 미래 더 따뜻한 학습
  데이터 확보 시 외삽 완화. 현 데이터로는 구조 완성 + 한계 명시가 정직.

### 가이드 매칭

- §5.4 climate_correct(v_table, scenario, future_age) = V_table + residual
  *구현 완료*.
- §3.1 SSP 시나리오 (NEX-GDDP-CMIP6 GEE 직접) *구현*.
- §8.2 growth_predict climate_scenario 파라미터 *작동* (baseline + SSP).
- §10.3 "baseline 도 valid" → 우리는 baseline + SSP 보정 + 외삽 정직 명시.

### 모듈 B 완성

D15 로 growth_predict() 가 성장(임분수확표) + 탄소(국립산림과학원) +
등급분포(Weibull D14) + 기후보정(climate_correct D13-D17 + NEX D15) 통합.
가이드 §1.2 모듈 B 책임 + §8.1 GrowthForecast 전 필드 충족.

### 후속

- (선택) 시각화: 차수별 잔차 추세, Weibull 곡선, SSP 보정 비교 (발표 Figure).
- README 13/13 갱신.

---

## (앞으로 추가 — 결정마다)